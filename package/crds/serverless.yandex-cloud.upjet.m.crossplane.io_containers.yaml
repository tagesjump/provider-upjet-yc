---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: containers.serverless.yandex-cloud.upjet.m.crossplane.io
spec:
  group: serverless.yandex-cloud.upjet.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - yandex-cloud
    kind: Container
    listKind: ContainerList
    plural: containers
    singular: container
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Container is the Schema for the Containers API. Allows management
          of a Yandex Cloud Serverless Container.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ContainerSpec defines the desired state of Container
            properties:
              forProvider:
                properties:
                  concurrency:
                    description: Concurrency of Yandex Cloud Serverless Container
                    type: number
                  connectivity:
                    description: Network access. If specified the revision will be
                      attached to specified network
                    items:
                      properties:
                        networkId:
                          description: Network the revision will have access to
                          type: string
                      type: object
                    type: array
                  coreFraction:
                    description: Core fraction (0...100) of the Yandex Cloud Serverless
                      Container
                    type: number
                  cores:
                    type: number
                  description:
                    description: Description of the Yandex Cloud Serverless Container
                    type: string
                  executionTimeout:
                    description: Execution timeout in seconds (duration format) for
                      Yandex Cloud Serverless Container
                    type: string
                  folderId:
                    description: Folder ID for the Yandex Cloud Serverless Container
                    type: string
                  folderIdRef:
                    description: Reference to a Folder in resourcemanager to populate
                      folderId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  folderIdSelector:
                    description: Selector for a Folder in resourcemanager to populate
                      folderId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  image:
                    description: Revision deployment image for Yandex Cloud Serverless
                      Container
                    items:
                      properties:
                        args:
                          description: List of arguments for Yandex Cloud Serverless
                            Container
                          items:
                            type: string
                          type: array
                        command:
                          description: List of commands for Yandex Cloud Serverless
                            Container
                          items:
                            type: string
                          type: array
                        digest:
                          description: |-
                            Digest of image that will be deployed as Yandex Cloud Serverless Container.
                            If presented, should be equal to digest that will be resolved at server side by URL.
                            Container will be updated on digest change even if image.0.url stays the same.
                            If field not specified then its value will be computed.
                          type: string
                        environment:
                          additionalProperties:
                            type: string
                          description: A set of key/value environment variable pairs
                            for Yandex Cloud Serverless Container
                          type: object
                          x-kubernetes-map-type: granular
                        url:
                          description: Invoke URL for the Yandex Cloud Serverless
                            Container
                          type: string
                        workDir:
                          description: Working directory for Yandex Cloud Serverless
                            Container
                          type: string
                      type: object
                    type: array
                  labels:
                    additionalProperties:
                      type: string
                    description: A set of key/value label pairs to assign to the Yandex
                      Cloud Serverless Container
                    type: object
                    x-kubernetes-map-type: granular
                  logOptions:
                    description: Options for logging from Yandex Cloud Serverless
                      Container
                    items:
                      properties:
                        disabled:
                          description: Is logging from container disabled
                          type: boolean
                        folderId:
                          description: Log entries are written to default log group
                            for specified folder
                          type: string
                        logGroupId:
                          description: Log entries are written to specified log group
                          type: string
                        minLevel:
                          description: Minimum log entry level
                          type: string
                      type: object
                    type: array
                  memory:
                    description: |-
                      Memory in megabytes (aligned to 128MB) for Yandex Cloud Serverless Container
                      Container memory in megabytes, should be aligned to 128
                    type: number
                  name:
                    description: Yandex Cloud Serverless Container name
                    type: string
                  secrets:
                    description: Secrets for Yandex Cloud Serverless Container
                    items:
                      properties:
                        environmentVariable:
                          description: Container's environment variable in which secret's
                            value will be stored.
                          type: string
                        id:
                          description: Secret's id.
                          type: string
                        key:
                          description: Secret's entries key which value will be stored
                            in environment variable.
                          type: string
                        versionId:
                          description: Secret's version id.
                          type: string
                      type: object
                    type: array
                  serviceAccountId:
                    description: Service account ID for Yandex Cloud Serverless Container
                    type: string
                  storageMounts:
                    description: Storage mounts for Yandex Cloud Serverless Container
                    items:
                      properties:
                        bucket:
                          description: Name of the mounting bucket.
                          type: string
                        mountPointPath:
                          description: Path inside the container to access the directory
                            in which the bucket is mounted.
                          type: string
                        prefix:
                          description: Prefix within the bucket. If you leave this
                            field empty, the entire bucket will be mounted.
                          type: string
                        readOnly:
                          description: Mount the bucket in read-only mode.
                          type: boolean
                      type: object
                    type: array
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  concurrency:
                    description: Concurrency of Yandex Cloud Serverless Container
                    type: number
                  connectivity:
                    description: Network access. If specified the revision will be
                      attached to specified network
                    items:
                      properties:
                        networkId:
                          description: Network the revision will have access to
                          type: string
                      type: object
                    type: array
                  coreFraction:
                    description: Core fraction (0...100) of the Yandex Cloud Serverless
                      Container
                    type: number
                  cores:
                    type: number
                  description:
                    description: Description of the Yandex Cloud Serverless Container
                    type: string
                  executionTimeout:
                    description: Execution timeout in seconds (duration format) for
                      Yandex Cloud Serverless Container
                    type: string
                  folderId:
                    description: Folder ID for the Yandex Cloud Serverless Container
                    type: string
                  folderIdRef:
                    description: Reference to a Folder in resourcemanager to populate
                      folderId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  folderIdSelector:
                    description: Selector for a Folder in resourcemanager to populate
                      folderId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  image:
                    description: Revision deployment image for Yandex Cloud Serverless
                      Container
                    items:
                      properties:
                        args:
                          description: List of arguments for Yandex Cloud Serverless
                            Container
                          items:
                            type: string
                          type: array
                        command:
                          description: List of commands for Yandex Cloud Serverless
                            Container
                          items:
                            type: string
                          type: array
                        digest:
                          description: |-
                            Digest of image that will be deployed as Yandex Cloud Serverless Container.
                            If presented, should be equal to digest that will be resolved at server side by URL.
                            Container will be updated on digest change even if image.0.url stays the same.
                            If field not specified then its value will be computed.
                          type: string
                        environment:
                          additionalProperties:
                            type: string
                          description: A set of key/value environment variable pairs
                            for Yandex Cloud Serverless Container
                          type: object
                          x-kubernetes-map-type: granular
                        url:
                          description: Invoke URL for the Yandex Cloud Serverless
                            Container
                          type: string
                        workDir:
                          description: Working directory for Yandex Cloud Serverless
                            Container
                          type: string
                      type: object
                    type: array
                  labels:
                    additionalProperties:
                      type: string
                    description: A set of key/value label pairs to assign to the Yandex
                      Cloud Serverless Container
                    type: object
                    x-kubernetes-map-type: granular
                  logOptions:
                    description: Options for logging from Yandex Cloud Serverless
                      Container
                    items:
                      properties:
                        disabled:
                          description: Is logging from container disabled
                          type: boolean
                        folderId:
                          description: Log entries are written to default log group
                            for specified folder
                          type: string
                        logGroupId:
                          description: Log entries are written to specified log group
                          type: string
                        minLevel:
                          description: Minimum log entry level
                          type: string
                      type: object
                    type: array
                  memory:
                    description: |-
                      Memory in megabytes (aligned to 128MB) for Yandex Cloud Serverless Container
                      Container memory in megabytes, should be aligned to 128
                    type: number
                  name:
                    description: Yandex Cloud Serverless Container name
                    type: string
                  secrets:
                    description: Secrets for Yandex Cloud Serverless Container
                    items:
                      properties:
                        environmentVariable:
                          description: Container's environment variable in which secret's
                            value will be stored.
                          type: string
                        id:
                          description: Secret's id.
                          type: string
                        key:
                          description: Secret's entries key which value will be stored
                            in environment variable.
                          type: string
                        versionId:
                          description: Secret's version id.
                          type: string
                      type: object
                    type: array
                  serviceAccountId:
                    description: Service account ID for Yandex Cloud Serverless Container
                    type: string
                  storageMounts:
                    description: Storage mounts for Yandex Cloud Serverless Container
                    items:
                      properties:
                        bucket:
                          description: Name of the mounting bucket.
                          type: string
                        mountPointPath:
                          description: Path inside the container to access the directory
                            in which the bucket is mounted.
                          type: string
                        prefix:
                          description: Prefix within the bucket. If you leave this
                            field empty, the entire bucket will be mounted.
                          type: string
                        readOnly:
                          description: Mount the bucket in read-only mode.
                          type: boolean
                      type: object
                    type: array
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.image is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.image)
                || (has(self.initProvider) && has(self.initProvider.image))'
            - message: spec.forProvider.memory is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.memory)
                || (has(self.initProvider) && has(self.initProvider.memory))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
          status:
            description: ContainerStatus defines the observed state of Container.
            properties:
              atProvider:
                properties:
                  concurrency:
                    description: Concurrency of Yandex Cloud Serverless Container
                    type: number
                  connectivity:
                    description: Network access. If specified the revision will be
                      attached to specified network
                    items:
                      properties:
                        networkId:
                          description: Network the revision will have access to
                          type: string
                      type: object
                    type: array
                  coreFraction:
                    description: Core fraction (0...100) of the Yandex Cloud Serverless
                      Container
                    type: number
                  cores:
                    type: number
                  createdAt:
                    description: Creation timestamp of the Yandex Cloud Serverless
                      Container
                    type: string
                  description:
                    description: Description of the Yandex Cloud Serverless Container
                    type: string
                  executionTimeout:
                    description: Execution timeout in seconds (duration format) for
                      Yandex Cloud Serverless Container
                    type: string
                  folderId:
                    description: Folder ID for the Yandex Cloud Serverless Container
                    type: string
                  id:
                    description: Secret's id.
                    type: string
                  image:
                    description: Revision deployment image for Yandex Cloud Serverless
                      Container
                    items:
                      properties:
                        args:
                          description: List of arguments for Yandex Cloud Serverless
                            Container
                          items:
                            type: string
                          type: array
                        command:
                          description: List of commands for Yandex Cloud Serverless
                            Container
                          items:
                            type: string
                          type: array
                        digest:
                          description: |-
                            Digest of image that will be deployed as Yandex Cloud Serverless Container.
                            If presented, should be equal to digest that will be resolved at server side by URL.
                            Container will be updated on digest change even if image.0.url stays the same.
                            If field not specified then its value will be computed.
                          type: string
                        environment:
                          additionalProperties:
                            type: string
                          description: A set of key/value environment variable pairs
                            for Yandex Cloud Serverless Container
                          type: object
                          x-kubernetes-map-type: granular
                        url:
                          description: Invoke URL for the Yandex Cloud Serverless
                            Container
                          type: string
                        workDir:
                          description: Working directory for Yandex Cloud Serverless
                            Container
                          type: string
                      type: object
                    type: array
                  labels:
                    additionalProperties:
                      type: string
                    description: A set of key/value label pairs to assign to the Yandex
                      Cloud Serverless Container
                    type: object
                    x-kubernetes-map-type: granular
                  logOptions:
                    description: Options for logging from Yandex Cloud Serverless
                      Container
                    items:
                      properties:
                        disabled:
                          description: Is logging from container disabled
                          type: boolean
                        folderId:
                          description: Log entries are written to default log group
                            for specified folder
                          type: string
                        logGroupId:
                          description: Log entries are written to specified log group
                          type: string
                        minLevel:
                          description: Minimum log entry level
                          type: string
                      type: object
                    type: array
                  memory:
                    description: |-
                      Memory in megabytes (aligned to 128MB) for Yandex Cloud Serverless Container
                      Container memory in megabytes, should be aligned to 128
                    type: number
                  name:
                    description: Yandex Cloud Serverless Container name
                    type: string
                  revisionId:
                    description: Last revision ID of the Yandex Cloud Serverless Container
                    type: string
                  secrets:
                    description: Secrets for Yandex Cloud Serverless Container
                    items:
                      properties:
                        environmentVariable:
                          description: Container's environment variable in which secret's
                            value will be stored.
                          type: string
                        id:
                          description: Secret's id.
                          type: string
                        key:
                          description: Secret's entries key which value will be stored
                            in environment variable.
                          type: string
                        versionId:
                          description: Secret's version id.
                          type: string
                      type: object
                    type: array
                  serviceAccountId:
                    description: Service account ID for Yandex Cloud Serverless Container
                    type: string
                  storageMounts:
                    description: Storage mounts for Yandex Cloud Serverless Container
                    items:
                      properties:
                        bucket:
                          description: Name of the mounting bucket.
                          type: string
                        mountPointPath:
                          description: Path inside the container to access the directory
                            in which the bucket is mounted.
                          type: string
                        prefix:
                          description: Prefix within the bucket. If you leave this
                            field empty, the entire bucket will be mounted.
                          type: string
                        readOnly:
                          description: Mount the bucket in read-only mode.
                          type: boolean
                      type: object
                    type: array
                  url:
                    description: Invoke URL for the Yandex Cloud Serverless Container
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
