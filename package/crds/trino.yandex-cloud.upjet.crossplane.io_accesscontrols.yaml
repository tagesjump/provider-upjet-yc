---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: accesscontrols.trino.yandex-cloud.upjet.crossplane.io
spec:
  group: trino.yandex-cloud.upjet.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - yandex-cloud
    kind: AccessControl
    listKind: AccessControlList
    plural: accesscontrols
    singular: accesscontrol
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AccessControl is the Schema for the AccessControls API. Manages
          Trino access control within Yandex Cloud.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AccessControlSpec defines the desired state of AccessControl
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  catalogSessionProperties:
                    description: (Attributes List) Catalog session property access
                      control rules. (see below for nested schema)
                    items:
                      properties:
                        allow:
                          description: |-
                            (String) Whether the rule allows setting the property. Valid values: NO, YES
                            Whether the rule allows setting the property. Valid values: `NO`, `YES`
                          type: string
                        catalog:
                          description: (Attributes) Catalog matcher specifying what
                            catalogs the rule is applied to. Exactly one of name_regexp,
                            ids attributes should be set. (see below for nested schema)
                          properties:
                            ids:
                              description: |-
                                (List of String) Catalog IDs rule is applied to.
                                Catalog IDs rule is applied to.
                              items:
                                type: string
                              type: array
                            nameRegexp:
                              description: |-
                                (String) Catalog name regexp the rule is applied to.
                                Catalog name regexp the rule is applied to.
                              type: string
                          type: object
                        description:
                          description: |-
                            (String) Rule description.
                            Rule description.
                          type: string
                        groups:
                          description: |-
                            (List of String) IAM group IDs the rule is applied to.
                            IAM group IDs the rule is applied to.
                          items:
                            type: string
                          type: array
                        property:
                          description: (Attributes) Matcher specifying what properties
                            the rule is applied to. Exactly one of name_regexp, names
                            attributes should be set. (see below for nested schema)
                          properties:
                            nameRegexp:
                              description: |-
                                (String) Catalog name regexp the rule is applied to.
                                Property name regexp the rule is applied to.
                              type: string
                            names:
                              description: |-
                                (List of String) Property names rule is applied to.
                                Property names rule is applied to.
                              items:
                                type: string
                              type: array
                          type: object
                        users:
                          description: |-
                            (List of String) IAM user IDs the rule is applied to.
                            IAM user IDs the rule is applied to.
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  catalogs:
                    description: (Attributes List) Catalog level access control rules.
                      (see below for nested schema)
                    items:
                      properties:
                        catalog:
                          description: (Attributes) Catalog matcher specifying what
                            catalogs the rule is applied to. Exactly one of name_regexp,
                            ids attributes should be set. (see below for nested schema)
                          properties:
                            ids:
                              description: |-
                                (List of String) Catalog IDs rule is applied to.
                                Catalog IDs rule is applied to.
                              items:
                                type: string
                              type: array
                            nameRegexp:
                              description: |-
                                (String) Catalog name regexp the rule is applied to.
                                Catalog name regexp the rule is applied to.
                              type: string
                          type: object
                        description:
                          description: |-
                            (String) Rule description.
                            Rule description.
                          type: string
                        groups:
                          description: |-
                            (List of String) IAM group IDs the rule is applied to.
                            IAM group IDs the rule is applied to.
                          items:
                            type: string
                          type: array
                        permission:
                          description: |-
                            (String) Permission granted by the rule. Valid values: NONE, READ_ONLY, ALL
                            Permission granted by the rule. Valid values: `NONE`, `READ_ONLY`, `ALL`
                          type: string
                        users:
                          description: |-
                            (List of String) IAM user IDs the rule is applied to.
                            IAM user IDs the rule is applied to.
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  clusterId:
                    description: |-
                      (String) ID of the Trino cluster. Provided by the client when the Access Control is created.
                      ID of the Trino cluster. Provided by the client when the Access Control is created.
                    type: string
                  clusterIdRef:
                    description: Reference to a Cluster in trino to populate clusterId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  clusterIdSelector:
                    description: Selector for a Cluster in trino to populate clusterId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  functions:
                    description: (Attributes List) Function level access control rules.
                      (see below for nested schema)
                    items:
                      properties:
                        catalog:
                          description: (Attributes) Catalog matcher specifying what
                            catalogs the rule is applied to. Exactly one of name_regexp,
                            ids attributes should be set. (see below for nested schema)
                          properties:
                            ids:
                              description: |-
                                (List of String) Catalog IDs rule is applied to.
                                Catalog IDs rule is applied to.
                              items:
                                type: string
                              type: array
                            nameRegexp:
                              description: |-
                                (String) Catalog name regexp the rule is applied to.
                                Catalog name regexp the rule is applied to.
                              type: string
                          type: object
                        description:
                          description: |-
                            (String) Rule description.
                            Rule description.
                          type: string
                        function:
                          description: (Attributes) Matcher specifying what functions
                            the rule is applied to. Exactly one of name_regexp, names
                            attributes should be set. (see below for nested schema)
                          properties:
                            nameRegexp:
                              description: |-
                                (String) Catalog name regexp the rule is applied to.
                                Function name regexp the rule is applied to.
                              type: string
                            names:
                              description: |-
                                (List of String) Property names rule is applied to.
                                Function names rule is applied to.
                              items:
                                type: string
                              type: array
                          type: object
                        groups:
                          description: |-
                            (List of String) IAM group IDs the rule is applied to.
                            IAM group IDs the rule is applied to.
                          items:
                            type: string
                          type: array
                        privileges:
                          description: |-
                            (List of String) Privileges granted by the rule. Valid values: EXECUTE, GRANT_EXECUTE, OWNERSHIP.
                            Privileges granted by the rule. Valid values: `EXECUTE`, `GRANT_EXECUTE`, `OWNERSHIP`.
                          items:
                            type: string
                          type: array
                        schema:
                          description: (Attributes) Matcher specifying what Schemas
                            the rule is applied to. Exactly one of name_regexp, names
                            attributes should be set. (see below for nested schema)
                          properties:
                            nameRegexp:
                              description: |-
                                (String) Catalog name regexp the rule is applied to.
                                Schema name regexp the rule is applied to.
                              type: string
                            names:
                              description: |-
                                (List of String) Property names rule is applied to.
                                Schema names rule is applied to.
                              items:
                                type: string
                              type: array
                          type: object
                        users:
                          description: |-
                            (List of String) IAM user IDs the rule is applied to.
                            IAM user IDs the rule is applied to.
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  procedures:
                    description: (Attributes List) Procedure level access control
                      rules. (see below for nested schema)
                    items:
                      properties:
                        catalog:
                          description: (Attributes) Catalog matcher specifying what
                            catalogs the rule is applied to. Exactly one of name_regexp,
                            ids attributes should be set. (see below for nested schema)
                          properties:
                            ids:
                              description: |-
                                (List of String) Catalog IDs rule is applied to.
                                Catalog IDs rule is applied to.
                              items:
                                type: string
                              type: array
                            nameRegexp:
                              description: |-
                                (String) Catalog name regexp the rule is applied to.
                                Catalog name regexp the rule is applied to.
                              type: string
                          type: object
                        description:
                          description: |-
                            (String) Rule description.
                            Rule description.
                          type: string
                        groups:
                          description: |-
                            (List of String) IAM group IDs the rule is applied to.
                            IAM group IDs the rule is applied to.
                          items:
                            type: string
                          type: array
                        privileges:
                          description: |-
                            (List of String) Privileges granted by the rule. Valid values: EXECUTE, GRANT_EXECUTE, OWNERSHIP.
                            Privileges granted by the rule. Valid values: `EXECUTE`.
                          items:
                            type: string
                          type: array
                        procedure:
                          description: (Attributes) Matcher specifying what procedures
                            the rule is applied to. Exactly one of name_regexp, names
                            attributes should be set. (see below for nested schema)
                          properties:
                            nameRegexp:
                              description: |-
                                (String) Catalog name regexp the rule is applied to.
                                Procedure name regexp the rule is applied to.
                              type: string
                            names:
                              description: |-
                                (List of String) Property names rule is applied to.
                                Procedure names rule is applied to.
                              items:
                                type: string
                              type: array
                          type: object
                        schema:
                          description: (Attributes) Matcher specifying what Schemas
                            the rule is applied to. Exactly one of name_regexp, names
                            attributes should be set. (see below for nested schema)
                          properties:
                            nameRegexp:
                              description: |-
                                (String) Catalog name regexp the rule is applied to.
                                Schema name regexp the rule is applied to.
                              type: string
                            names:
                              description: |-
                                (List of String) Property names rule is applied to.
                                Schema names rule is applied to.
                              items:
                                type: string
                              type: array
                          type: object
                        users:
                          description: |-
                            (List of String) IAM user IDs the rule is applied to.
                            IAM user IDs the rule is applied to.
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  queries:
                    description: (Attributes List) Query level access control rules.
                      (see below for nested schema)
                    items:
                      properties:
                        description:
                          description: |-
                            (String) Rule description.
                            Rule description.
                          type: string
                        groups:
                          description: |-
                            (List of String) IAM group IDs the rule is applied to.
                            IAM group IDs the rule is applied to.
                          items:
                            type: string
                          type: array
                        privileges:
                          description: |-
                            (List of String) Privileges granted by the rule. Valid values: EXECUTE, GRANT_EXECUTE, OWNERSHIP.
                            Privileges granted by the rule. Valid values: `VIEW`, `EXECUTE`, `KILL`.
                          items:
                            type: string
                          type: array
                        queryOwners:
                          description: |-
                            (List of String) Owners of queries the rule is applied to.
                            Owners of queries the rule is applied to.
                          items:
                            type: string
                          type: array
                        users:
                          description: |-
                            (List of String) IAM user IDs the rule is applied to.
                            IAM user IDs the rule is applied to.
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  schemas:
                    description: (Attributes List) Schema level access control rules.
                      (see below for nested schema)
                    items:
                      properties:
                        catalog:
                          description: (Attributes) Catalog matcher specifying what
                            catalogs the rule is applied to. Exactly one of name_regexp,
                            ids attributes should be set. (see below for nested schema)
                          properties:
                            ids:
                              description: |-
                                (List of String) Catalog IDs rule is applied to.
                                Catalog IDs rule is applied to.
                              items:
                                type: string
                              type: array
                            nameRegexp:
                              description: |-
                                (String) Catalog name regexp the rule is applied to.
                                Catalog name regexp the rule is applied to.
                              type: string
                          type: object
                        description:
                          description: |-
                            (String) Rule description.
                            Rule description.
                          type: string
                        groups:
                          description: |-
                            (List of String) IAM group IDs the rule is applied to.
                            IAM group IDs the rule is applied to.
                          items:
                            type: string
                          type: array
                        owner:
                          description: |-
                            (String) Ownership granted by the rule. Valid values: NO, YES
                            Ownership granted by the rule. Valid values: `NO`, `YES`
                          type: string
                        schema:
                          description: (Attributes) Matcher specifying what Schemas
                            the rule is applied to. Exactly one of name_regexp, names
                            attributes should be set. (see below for nested schema)
                          properties:
                            nameRegexp:
                              description: |-
                                (String) Catalog name regexp the rule is applied to.
                                Schema name regexp the rule is applied to.
                              type: string
                            names:
                              description: |-
                                (List of String) Property names rule is applied to.
                                Schema names rule is applied to.
                              items:
                                type: string
                              type: array
                          type: object
                        users:
                          description: |-
                            (List of String) IAM user IDs the rule is applied to.
                            IAM user IDs the rule is applied to.
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  systemSessionProperties:
                    description: (Attributes List) System session property access
                      control rules. (see below for nested schema)
                    items:
                      properties:
                        allow:
                          description: |-
                            (String) Whether the rule allows setting the property. Valid values: NO, YES
                            Whether the rule allows setting the property. Valid values: `NO`, `YES`
                          type: string
                        description:
                          description: |-
                            (String) Rule description.
                            Rule description.
                          type: string
                        groups:
                          description: |-
                            (List of String) IAM group IDs the rule is applied to.
                            IAM group IDs the rule is applied to.
                          items:
                            type: string
                          type: array
                        property:
                          description: (Attributes) Matcher specifying what properties
                            the rule is applied to. Exactly one of name_regexp, names
                            attributes should be set. (see below for nested schema)
                          properties:
                            nameRegexp:
                              description: |-
                                (String) Catalog name regexp the rule is applied to.
                                Property name regexp the rule is applied to.
                              type: string
                            names:
                              description: |-
                                (List of String) Property names rule is applied to.
                                Property names rule is applied to.
                              items:
                                type: string
                              type: array
                          type: object
                        users:
                          description: |-
                            (List of String) IAM user IDs the rule is applied to.
                            IAM user IDs the rule is applied to.
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  tables:
                    description: (Attributes List) Table level access control rules.
                      (see below for nested schema)
                    items:
                      properties:
                        catalog:
                          description: (Attributes) Catalog matcher specifying what
                            catalogs the rule is applied to. Exactly one of name_regexp,
                            ids attributes should be set. (see below for nested schema)
                          properties:
                            ids:
                              description: |-
                                (List of String) Catalog IDs rule is applied to.
                                Catalog IDs rule is applied to.
                              items:
                                type: string
                              type: array
                            nameRegexp:
                              description: |-
                                (String) Catalog name regexp the rule is applied to.
                                Catalog name regexp the rule is applied to.
                              type: string
                          type: object
                        columns:
                          description: (Attributes List) Column rules. (see below
                            for nested schema)
                          items:
                            properties:
                              access:
                                description: |-
                                  (String) Column access mode. Valid values: NONE, ALL
                                  Column access mode. Valid values: `NONE`, `ALL`
                                type: string
                              mask:
                                description: |-
                                  (String) SQL expression mask to evaluate instead of original column values.
                                  SQL expression mask to evaluate instead of original column values.
                                type: string
                              name:
                                description: |-
                                  (String) Column name.
                                  Column name.
                                type: string
                            type: object
                          type: array
                        description:
                          description: |-
                            (String) Rule description.
                            Rule description.
                          type: string
                        filter:
                          description: |-
                            (String) Boolean SQL expression to filter table rows for particular user.
                            Boolean SQL expression to filter table rows for particular user.
                          type: string
                        groups:
                          description: |-
                            (List of String) IAM group IDs the rule is applied to.
                            IAM group IDs the rule is applied to.
                          items:
                            type: string
                          type: array
                        privileges:
                          description: |-
                            (List of String) Privileges granted by the rule. Valid values: EXECUTE, GRANT_EXECUTE, OWNERSHIP.
                            Privileges granted by the rule. Valid values: `SELECT`, `INSERT`, `DELETE`, `UPDATE`, `OWNERSHIP`, `GRANT_SELECT`.
                          items:
                            type: string
                          type: array
                        schema:
                          description: (Attributes) Matcher specifying what Schemas
                            the rule is applied to. Exactly one of name_regexp, names
                            attributes should be set. (see below for nested schema)
                          properties:
                            nameRegexp:
                              description: |-
                                (String) Catalog name regexp the rule is applied to.
                                Schema name regexp the rule is applied to.
                              type: string
                            names:
                              description: |-
                                (List of String) Property names rule is applied to.
                                Schema names rule is applied to.
                              items:
                                type: string
                              type: array
                          type: object
                        table:
                          description: (Attributes) Matcher specifying what tables
                            the rule is applied to. Exactly one of name_regexp, names
                            attributes should be set. (see below for nested schema)
                          properties:
                            nameRegexp:
                              description: |-
                                (String) Catalog name regexp the rule is applied to.
                                Table name regexp the rule is applied to.
                              type: string
                            names:
                              description: |-
                                (List of String) Property names rule is applied to.
                                Table names rule is applied to.
                              items:
                                type: string
                              type: array
                          type: object
                        users:
                          description: |-
                            (List of String) IAM user IDs the rule is applied to.
                            IAM user IDs the rule is applied to.
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  catalogSessionProperties:
                    description: (Attributes List) Catalog session property access
                      control rules. (see below for nested schema)
                    items:
                      properties:
                        allow:
                          description: |-
                            (String) Whether the rule allows setting the property. Valid values: NO, YES
                            Whether the rule allows setting the property. Valid values: `NO`, `YES`
                          type: string
                        catalog:
                          description: (Attributes) Catalog matcher specifying what
                            catalogs the rule is applied to. Exactly one of name_regexp,
                            ids attributes should be set. (see below for nested schema)
                          properties:
                            ids:
                              description: |-
                                (List of String) Catalog IDs rule is applied to.
                                Catalog IDs rule is applied to.
                              items:
                                type: string
                              type: array
                            nameRegexp:
                              description: |-
                                (String) Catalog name regexp the rule is applied to.
                                Catalog name regexp the rule is applied to.
                              type: string
                          type: object
                        description:
                          description: |-
                            (String) Rule description.
                            Rule description.
                          type: string
                        groups:
                          description: |-
                            (List of String) IAM group IDs the rule is applied to.
                            IAM group IDs the rule is applied to.
                          items:
                            type: string
                          type: array
                        property:
                          description: (Attributes) Matcher specifying what properties
                            the rule is applied to. Exactly one of name_regexp, names
                            attributes should be set. (see below for nested schema)
                          properties:
                            nameRegexp:
                              description: |-
                                (String) Catalog name regexp the rule is applied to.
                                Property name regexp the rule is applied to.
                              type: string
                            names:
                              description: |-
                                (List of String) Property names rule is applied to.
                                Property names rule is applied to.
                              items:
                                type: string
                              type: array
                          type: object
                        users:
                          description: |-
                            (List of String) IAM user IDs the rule is applied to.
                            IAM user IDs the rule is applied to.
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  catalogs:
                    description: (Attributes List) Catalog level access control rules.
                      (see below for nested schema)
                    items:
                      properties:
                        catalog:
                          description: (Attributes) Catalog matcher specifying what
                            catalogs the rule is applied to. Exactly one of name_regexp,
                            ids attributes should be set. (see below for nested schema)
                          properties:
                            ids:
                              description: |-
                                (List of String) Catalog IDs rule is applied to.
                                Catalog IDs rule is applied to.
                              items:
                                type: string
                              type: array
                            nameRegexp:
                              description: |-
                                (String) Catalog name regexp the rule is applied to.
                                Catalog name regexp the rule is applied to.
                              type: string
                          type: object
                        description:
                          description: |-
                            (String) Rule description.
                            Rule description.
                          type: string
                        groups:
                          description: |-
                            (List of String) IAM group IDs the rule is applied to.
                            IAM group IDs the rule is applied to.
                          items:
                            type: string
                          type: array
                        permission:
                          description: |-
                            (String) Permission granted by the rule. Valid values: NONE, READ_ONLY, ALL
                            Permission granted by the rule. Valid values: `NONE`, `READ_ONLY`, `ALL`
                          type: string
                        users:
                          description: |-
                            (List of String) IAM user IDs the rule is applied to.
                            IAM user IDs the rule is applied to.
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  clusterId:
                    description: |-
                      (String) ID of the Trino cluster. Provided by the client when the Access Control is created.
                      ID of the Trino cluster. Provided by the client when the Access Control is created.
                    type: string
                  clusterIdRef:
                    description: Reference to a Cluster in trino to populate clusterId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  clusterIdSelector:
                    description: Selector for a Cluster in trino to populate clusterId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  functions:
                    description: (Attributes List) Function level access control rules.
                      (see below for nested schema)
                    items:
                      properties:
                        catalog:
                          description: (Attributes) Catalog matcher specifying what
                            catalogs the rule is applied to. Exactly one of name_regexp,
                            ids attributes should be set. (see below for nested schema)
                          properties:
                            ids:
                              description: |-
                                (List of String) Catalog IDs rule is applied to.
                                Catalog IDs rule is applied to.
                              items:
                                type: string
                              type: array
                            nameRegexp:
                              description: |-
                                (String) Catalog name regexp the rule is applied to.
                                Catalog name regexp the rule is applied to.
                              type: string
                          type: object
                        description:
                          description: |-
                            (String) Rule description.
                            Rule description.
                          type: string
                        function:
                          description: (Attributes) Matcher specifying what functions
                            the rule is applied to. Exactly one of name_regexp, names
                            attributes should be set. (see below for nested schema)
                          properties:
                            nameRegexp:
                              description: |-
                                (String) Catalog name regexp the rule is applied to.
                                Function name regexp the rule is applied to.
                              type: string
                            names:
                              description: |-
                                (List of String) Property names rule is applied to.
                                Function names rule is applied to.
                              items:
                                type: string
                              type: array
                          type: object
                        groups:
                          description: |-
                            (List of String) IAM group IDs the rule is applied to.
                            IAM group IDs the rule is applied to.
                          items:
                            type: string
                          type: array
                        privileges:
                          description: |-
                            (List of String) Privileges granted by the rule. Valid values: EXECUTE, GRANT_EXECUTE, OWNERSHIP.
                            Privileges granted by the rule. Valid values: `EXECUTE`, `GRANT_EXECUTE`, `OWNERSHIP`.
                          items:
                            type: string
                          type: array
                        schema:
                          description: (Attributes) Matcher specifying what Schemas
                            the rule is applied to. Exactly one of name_regexp, names
                            attributes should be set. (see below for nested schema)
                          properties:
                            nameRegexp:
                              description: |-
                                (String) Catalog name regexp the rule is applied to.
                                Schema name regexp the rule is applied to.
                              type: string
                            names:
                              description: |-
                                (List of String) Property names rule is applied to.
                                Schema names rule is applied to.
                              items:
                                type: string
                              type: array
                          type: object
                        users:
                          description: |-
                            (List of String) IAM user IDs the rule is applied to.
                            IAM user IDs the rule is applied to.
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  procedures:
                    description: (Attributes List) Procedure level access control
                      rules. (see below for nested schema)
                    items:
                      properties:
                        catalog:
                          description: (Attributes) Catalog matcher specifying what
                            catalogs the rule is applied to. Exactly one of name_regexp,
                            ids attributes should be set. (see below for nested schema)
                          properties:
                            ids:
                              description: |-
                                (List of String) Catalog IDs rule is applied to.
                                Catalog IDs rule is applied to.
                              items:
                                type: string
                              type: array
                            nameRegexp:
                              description: |-
                                (String) Catalog name regexp the rule is applied to.
                                Catalog name regexp the rule is applied to.
                              type: string
                          type: object
                        description:
                          description: |-
                            (String) Rule description.
                            Rule description.
                          type: string
                        groups:
                          description: |-
                            (List of String) IAM group IDs the rule is applied to.
                            IAM group IDs the rule is applied to.
                          items:
                            type: string
                          type: array
                        privileges:
                          description: |-
                            (List of String) Privileges granted by the rule. Valid values: EXECUTE, GRANT_EXECUTE, OWNERSHIP.
                            Privileges granted by the rule. Valid values: `EXECUTE`.
                          items:
                            type: string
                          type: array
                        procedure:
                          description: (Attributes) Matcher specifying what procedures
                            the rule is applied to. Exactly one of name_regexp, names
                            attributes should be set. (see below for nested schema)
                          properties:
                            nameRegexp:
                              description: |-
                                (String) Catalog name regexp the rule is applied to.
                                Procedure name regexp the rule is applied to.
                              type: string
                            names:
                              description: |-
                                (List of String) Property names rule is applied to.
                                Procedure names rule is applied to.
                              items:
                                type: string
                              type: array
                          type: object
                        schema:
                          description: (Attributes) Matcher specifying what Schemas
                            the rule is applied to. Exactly one of name_regexp, names
                            attributes should be set. (see below for nested schema)
                          properties:
                            nameRegexp:
                              description: |-
                                (String) Catalog name regexp the rule is applied to.
                                Schema name regexp the rule is applied to.
                              type: string
                            names:
                              description: |-
                                (List of String) Property names rule is applied to.
                                Schema names rule is applied to.
                              items:
                                type: string
                              type: array
                          type: object
                        users:
                          description: |-
                            (List of String) IAM user IDs the rule is applied to.
                            IAM user IDs the rule is applied to.
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  queries:
                    description: (Attributes List) Query level access control rules.
                      (see below for nested schema)
                    items:
                      properties:
                        description:
                          description: |-
                            (String) Rule description.
                            Rule description.
                          type: string
                        groups:
                          description: |-
                            (List of String) IAM group IDs the rule is applied to.
                            IAM group IDs the rule is applied to.
                          items:
                            type: string
                          type: array
                        privileges:
                          description: |-
                            (List of String) Privileges granted by the rule. Valid values: EXECUTE, GRANT_EXECUTE, OWNERSHIP.
                            Privileges granted by the rule. Valid values: `VIEW`, `EXECUTE`, `KILL`.
                          items:
                            type: string
                          type: array
                        queryOwners:
                          description: |-
                            (List of String) Owners of queries the rule is applied to.
                            Owners of queries the rule is applied to.
                          items:
                            type: string
                          type: array
                        users:
                          description: |-
                            (List of String) IAM user IDs the rule is applied to.
                            IAM user IDs the rule is applied to.
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  schemas:
                    description: (Attributes List) Schema level access control rules.
                      (see below for nested schema)
                    items:
                      properties:
                        catalog:
                          description: (Attributes) Catalog matcher specifying what
                            catalogs the rule is applied to. Exactly one of name_regexp,
                            ids attributes should be set. (see below for nested schema)
                          properties:
                            ids:
                              description: |-
                                (List of String) Catalog IDs rule is applied to.
                                Catalog IDs rule is applied to.
                              items:
                                type: string
                              type: array
                            nameRegexp:
                              description: |-
                                (String) Catalog name regexp the rule is applied to.
                                Catalog name regexp the rule is applied to.
                              type: string
                          type: object
                        description:
                          description: |-
                            (String) Rule description.
                            Rule description.
                          type: string
                        groups:
                          description: |-
                            (List of String) IAM group IDs the rule is applied to.
                            IAM group IDs the rule is applied to.
                          items:
                            type: string
                          type: array
                        owner:
                          description: |-
                            (String) Ownership granted by the rule. Valid values: NO, YES
                            Ownership granted by the rule. Valid values: `NO`, `YES`
                          type: string
                        schema:
                          description: (Attributes) Matcher specifying what Schemas
                            the rule is applied to. Exactly one of name_regexp, names
                            attributes should be set. (see below for nested schema)
                          properties:
                            nameRegexp:
                              description: |-
                                (String) Catalog name regexp the rule is applied to.
                                Schema name regexp the rule is applied to.
                              type: string
                            names:
                              description: |-
                                (List of String) Property names rule is applied to.
                                Schema names rule is applied to.
                              items:
                                type: string
                              type: array
                          type: object
                        users:
                          description: |-
                            (List of String) IAM user IDs the rule is applied to.
                            IAM user IDs the rule is applied to.
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  systemSessionProperties:
                    description: (Attributes List) System session property access
                      control rules. (see below for nested schema)
                    items:
                      properties:
                        allow:
                          description: |-
                            (String) Whether the rule allows setting the property. Valid values: NO, YES
                            Whether the rule allows setting the property. Valid values: `NO`, `YES`
                          type: string
                        description:
                          description: |-
                            (String) Rule description.
                            Rule description.
                          type: string
                        groups:
                          description: |-
                            (List of String) IAM group IDs the rule is applied to.
                            IAM group IDs the rule is applied to.
                          items:
                            type: string
                          type: array
                        property:
                          description: (Attributes) Matcher specifying what properties
                            the rule is applied to. Exactly one of name_regexp, names
                            attributes should be set. (see below for nested schema)
                          properties:
                            nameRegexp:
                              description: |-
                                (String) Catalog name regexp the rule is applied to.
                                Property name regexp the rule is applied to.
                              type: string
                            names:
                              description: |-
                                (List of String) Property names rule is applied to.
                                Property names rule is applied to.
                              items:
                                type: string
                              type: array
                          type: object
                        users:
                          description: |-
                            (List of String) IAM user IDs the rule is applied to.
                            IAM user IDs the rule is applied to.
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  tables:
                    description: (Attributes List) Table level access control rules.
                      (see below for nested schema)
                    items:
                      properties:
                        catalog:
                          description: (Attributes) Catalog matcher specifying what
                            catalogs the rule is applied to. Exactly one of name_regexp,
                            ids attributes should be set. (see below for nested schema)
                          properties:
                            ids:
                              description: |-
                                (List of String) Catalog IDs rule is applied to.
                                Catalog IDs rule is applied to.
                              items:
                                type: string
                              type: array
                            nameRegexp:
                              description: |-
                                (String) Catalog name regexp the rule is applied to.
                                Catalog name regexp the rule is applied to.
                              type: string
                          type: object
                        columns:
                          description: (Attributes List) Column rules. (see below
                            for nested schema)
                          items:
                            properties:
                              access:
                                description: |-
                                  (String) Column access mode. Valid values: NONE, ALL
                                  Column access mode. Valid values: `NONE`, `ALL`
                                type: string
                              mask:
                                description: |-
                                  (String) SQL expression mask to evaluate instead of original column values.
                                  SQL expression mask to evaluate instead of original column values.
                                type: string
                              name:
                                description: |-
                                  (String) Column name.
                                  Column name.
                                type: string
                            type: object
                          type: array
                        description:
                          description: |-
                            (String) Rule description.
                            Rule description.
                          type: string
                        filter:
                          description: |-
                            (String) Boolean SQL expression to filter table rows for particular user.
                            Boolean SQL expression to filter table rows for particular user.
                          type: string
                        groups:
                          description: |-
                            (List of String) IAM group IDs the rule is applied to.
                            IAM group IDs the rule is applied to.
                          items:
                            type: string
                          type: array
                        privileges:
                          description: |-
                            (List of String) Privileges granted by the rule. Valid values: EXECUTE, GRANT_EXECUTE, OWNERSHIP.
                            Privileges granted by the rule. Valid values: `SELECT`, `INSERT`, `DELETE`, `UPDATE`, `OWNERSHIP`, `GRANT_SELECT`.
                          items:
                            type: string
                          type: array
                        schema:
                          description: (Attributes) Matcher specifying what Schemas
                            the rule is applied to. Exactly one of name_regexp, names
                            attributes should be set. (see below for nested schema)
                          properties:
                            nameRegexp:
                              description: |-
                                (String) Catalog name regexp the rule is applied to.
                                Schema name regexp the rule is applied to.
                              type: string
                            names:
                              description: |-
                                (List of String) Property names rule is applied to.
                                Schema names rule is applied to.
                              items:
                                type: string
                              type: array
                          type: object
                        table:
                          description: (Attributes) Matcher specifying what tables
                            the rule is applied to. Exactly one of name_regexp, names
                            attributes should be set. (see below for nested schema)
                          properties:
                            nameRegexp:
                              description: |-
                                (String) Catalog name regexp the rule is applied to.
                                Table name regexp the rule is applied to.
                              type: string
                            names:
                              description: |-
                                (List of String) Property names rule is applied to.
                                Table names rule is applied to.
                              items:
                                type: string
                              type: array
                          type: object
                        users:
                          description: |-
                            (List of String) IAM user IDs the rule is applied to.
                            IAM user IDs the rule is applied to.
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
          status:
            description: AccessControlStatus defines the observed state of AccessControl.
            properties:
              atProvider:
                properties:
                  catalogSessionProperties:
                    description: (Attributes List) Catalog session property access
                      control rules. (see below for nested schema)
                    items:
                      properties:
                        allow:
                          description: |-
                            (String) Whether the rule allows setting the property. Valid values: NO, YES
                            Whether the rule allows setting the property. Valid values: `NO`, `YES`
                          type: string
                        catalog:
                          description: (Attributes) Catalog matcher specifying what
                            catalogs the rule is applied to. Exactly one of name_regexp,
                            ids attributes should be set. (see below for nested schema)
                          properties:
                            ids:
                              description: |-
                                (List of String) Catalog IDs rule is applied to.
                                Catalog IDs rule is applied to.
                              items:
                                type: string
                              type: array
                            nameRegexp:
                              description: |-
                                (String) Catalog name regexp the rule is applied to.
                                Catalog name regexp the rule is applied to.
                              type: string
                          type: object
                        description:
                          description: |-
                            (String) Rule description.
                            Rule description.
                          type: string
                        groups:
                          description: |-
                            (List of String) IAM group IDs the rule is applied to.
                            IAM group IDs the rule is applied to.
                          items:
                            type: string
                          type: array
                        property:
                          description: (Attributes) Matcher specifying what properties
                            the rule is applied to. Exactly one of name_regexp, names
                            attributes should be set. (see below for nested schema)
                          properties:
                            nameRegexp:
                              description: |-
                                (String) Catalog name regexp the rule is applied to.
                                Property name regexp the rule is applied to.
                              type: string
                            names:
                              description: |-
                                (List of String) Property names rule is applied to.
                                Property names rule is applied to.
                              items:
                                type: string
                              type: array
                          type: object
                        users:
                          description: |-
                            (List of String) IAM user IDs the rule is applied to.
                            IAM user IDs the rule is applied to.
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  catalogs:
                    description: (Attributes List) Catalog level access control rules.
                      (see below for nested schema)
                    items:
                      properties:
                        catalog:
                          description: (Attributes) Catalog matcher specifying what
                            catalogs the rule is applied to. Exactly one of name_regexp,
                            ids attributes should be set. (see below for nested schema)
                          properties:
                            ids:
                              description: |-
                                (List of String) Catalog IDs rule is applied to.
                                Catalog IDs rule is applied to.
                              items:
                                type: string
                              type: array
                            nameRegexp:
                              description: |-
                                (String) Catalog name regexp the rule is applied to.
                                Catalog name regexp the rule is applied to.
                              type: string
                          type: object
                        description:
                          description: |-
                            (String) Rule description.
                            Rule description.
                          type: string
                        groups:
                          description: |-
                            (List of String) IAM group IDs the rule is applied to.
                            IAM group IDs the rule is applied to.
                          items:
                            type: string
                          type: array
                        permission:
                          description: |-
                            (String) Permission granted by the rule. Valid values: NONE, READ_ONLY, ALL
                            Permission granted by the rule. Valid values: `NONE`, `READ_ONLY`, `ALL`
                          type: string
                        users:
                          description: |-
                            (List of String) IAM user IDs the rule is applied to.
                            IAM user IDs the rule is applied to.
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  clusterId:
                    description: |-
                      (String) ID of the Trino cluster. Provided by the client when the Access Control is created.
                      ID of the Trino cluster. Provided by the client when the Access Control is created.
                    type: string
                  functions:
                    description: (Attributes List) Function level access control rules.
                      (see below for nested schema)
                    items:
                      properties:
                        catalog:
                          description: (Attributes) Catalog matcher specifying what
                            catalogs the rule is applied to. Exactly one of name_regexp,
                            ids attributes should be set. (see below for nested schema)
                          properties:
                            ids:
                              description: |-
                                (List of String) Catalog IDs rule is applied to.
                                Catalog IDs rule is applied to.
                              items:
                                type: string
                              type: array
                            nameRegexp:
                              description: |-
                                (String) Catalog name regexp the rule is applied to.
                                Catalog name regexp the rule is applied to.
                              type: string
                          type: object
                        description:
                          description: |-
                            (String) Rule description.
                            Rule description.
                          type: string
                        function:
                          description: (Attributes) Matcher specifying what functions
                            the rule is applied to. Exactly one of name_regexp, names
                            attributes should be set. (see below for nested schema)
                          properties:
                            nameRegexp:
                              description: |-
                                (String) Catalog name regexp the rule is applied to.
                                Function name regexp the rule is applied to.
                              type: string
                            names:
                              description: |-
                                (List of String) Property names rule is applied to.
                                Function names rule is applied to.
                              items:
                                type: string
                              type: array
                          type: object
                        groups:
                          description: |-
                            (List of String) IAM group IDs the rule is applied to.
                            IAM group IDs the rule is applied to.
                          items:
                            type: string
                          type: array
                        privileges:
                          description: |-
                            (List of String) Privileges granted by the rule. Valid values: EXECUTE, GRANT_EXECUTE, OWNERSHIP.
                            Privileges granted by the rule. Valid values: `EXECUTE`, `GRANT_EXECUTE`, `OWNERSHIP`.
                          items:
                            type: string
                          type: array
                        schema:
                          description: (Attributes) Matcher specifying what Schemas
                            the rule is applied to. Exactly one of name_regexp, names
                            attributes should be set. (see below for nested schema)
                          properties:
                            nameRegexp:
                              description: |-
                                (String) Catalog name regexp the rule is applied to.
                                Schema name regexp the rule is applied to.
                              type: string
                            names:
                              description: |-
                                (List of String) Property names rule is applied to.
                                Schema names rule is applied to.
                              items:
                                type: string
                              type: array
                          type: object
                        users:
                          description: |-
                            (List of String) IAM user IDs the rule is applied to.
                            IAM user IDs the rule is applied to.
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  id:
                    type: string
                  procedures:
                    description: (Attributes List) Procedure level access control
                      rules. (see below for nested schema)
                    items:
                      properties:
                        catalog:
                          description: (Attributes) Catalog matcher specifying what
                            catalogs the rule is applied to. Exactly one of name_regexp,
                            ids attributes should be set. (see below for nested schema)
                          properties:
                            ids:
                              description: |-
                                (List of String) Catalog IDs rule is applied to.
                                Catalog IDs rule is applied to.
                              items:
                                type: string
                              type: array
                            nameRegexp:
                              description: |-
                                (String) Catalog name regexp the rule is applied to.
                                Catalog name regexp the rule is applied to.
                              type: string
                          type: object
                        description:
                          description: |-
                            (String) Rule description.
                            Rule description.
                          type: string
                        groups:
                          description: |-
                            (List of String) IAM group IDs the rule is applied to.
                            IAM group IDs the rule is applied to.
                          items:
                            type: string
                          type: array
                        privileges:
                          description: |-
                            (List of String) Privileges granted by the rule. Valid values: EXECUTE, GRANT_EXECUTE, OWNERSHIP.
                            Privileges granted by the rule. Valid values: `EXECUTE`.
                          items:
                            type: string
                          type: array
                        procedure:
                          description: (Attributes) Matcher specifying what procedures
                            the rule is applied to. Exactly one of name_regexp, names
                            attributes should be set. (see below for nested schema)
                          properties:
                            nameRegexp:
                              description: |-
                                (String) Catalog name regexp the rule is applied to.
                                Procedure name regexp the rule is applied to.
                              type: string
                            names:
                              description: |-
                                (List of String) Property names rule is applied to.
                                Procedure names rule is applied to.
                              items:
                                type: string
                              type: array
                          type: object
                        schema:
                          description: (Attributes) Matcher specifying what Schemas
                            the rule is applied to. Exactly one of name_regexp, names
                            attributes should be set. (see below for nested schema)
                          properties:
                            nameRegexp:
                              description: |-
                                (String) Catalog name regexp the rule is applied to.
                                Schema name regexp the rule is applied to.
                              type: string
                            names:
                              description: |-
                                (List of String) Property names rule is applied to.
                                Schema names rule is applied to.
                              items:
                                type: string
                              type: array
                          type: object
                        users:
                          description: |-
                            (List of String) IAM user IDs the rule is applied to.
                            IAM user IDs the rule is applied to.
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  queries:
                    description: (Attributes List) Query level access control rules.
                      (see below for nested schema)
                    items:
                      properties:
                        description:
                          description: |-
                            (String) Rule description.
                            Rule description.
                          type: string
                        groups:
                          description: |-
                            (List of String) IAM group IDs the rule is applied to.
                            IAM group IDs the rule is applied to.
                          items:
                            type: string
                          type: array
                        privileges:
                          description: |-
                            (List of String) Privileges granted by the rule. Valid values: EXECUTE, GRANT_EXECUTE, OWNERSHIP.
                            Privileges granted by the rule. Valid values: `VIEW`, `EXECUTE`, `KILL`.
                          items:
                            type: string
                          type: array
                        queryOwners:
                          description: |-
                            (List of String) Owners of queries the rule is applied to.
                            Owners of queries the rule is applied to.
                          items:
                            type: string
                          type: array
                        users:
                          description: |-
                            (List of String) IAM user IDs the rule is applied to.
                            IAM user IDs the rule is applied to.
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  schemas:
                    description: (Attributes List) Schema level access control rules.
                      (see below for nested schema)
                    items:
                      properties:
                        catalog:
                          description: (Attributes) Catalog matcher specifying what
                            catalogs the rule is applied to. Exactly one of name_regexp,
                            ids attributes should be set. (see below for nested schema)
                          properties:
                            ids:
                              description: |-
                                (List of String) Catalog IDs rule is applied to.
                                Catalog IDs rule is applied to.
                              items:
                                type: string
                              type: array
                            nameRegexp:
                              description: |-
                                (String) Catalog name regexp the rule is applied to.
                                Catalog name regexp the rule is applied to.
                              type: string
                          type: object
                        description:
                          description: |-
                            (String) Rule description.
                            Rule description.
                          type: string
                        groups:
                          description: |-
                            (List of String) IAM group IDs the rule is applied to.
                            IAM group IDs the rule is applied to.
                          items:
                            type: string
                          type: array
                        owner:
                          description: |-
                            (String) Ownership granted by the rule. Valid values: NO, YES
                            Ownership granted by the rule. Valid values: `NO`, `YES`
                          type: string
                        schema:
                          description: (Attributes) Matcher specifying what Schemas
                            the rule is applied to. Exactly one of name_regexp, names
                            attributes should be set. (see below for nested schema)
                          properties:
                            nameRegexp:
                              description: |-
                                (String) Catalog name regexp the rule is applied to.
                                Schema name regexp the rule is applied to.
                              type: string
                            names:
                              description: |-
                                (List of String) Property names rule is applied to.
                                Schema names rule is applied to.
                              items:
                                type: string
                              type: array
                          type: object
                        users:
                          description: |-
                            (List of String) IAM user IDs the rule is applied to.
                            IAM user IDs the rule is applied to.
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  systemSessionProperties:
                    description: (Attributes List) System session property access
                      control rules. (see below for nested schema)
                    items:
                      properties:
                        allow:
                          description: |-
                            (String) Whether the rule allows setting the property. Valid values: NO, YES
                            Whether the rule allows setting the property. Valid values: `NO`, `YES`
                          type: string
                        description:
                          description: |-
                            (String) Rule description.
                            Rule description.
                          type: string
                        groups:
                          description: |-
                            (List of String) IAM group IDs the rule is applied to.
                            IAM group IDs the rule is applied to.
                          items:
                            type: string
                          type: array
                        property:
                          description: (Attributes) Matcher specifying what properties
                            the rule is applied to. Exactly one of name_regexp, names
                            attributes should be set. (see below for nested schema)
                          properties:
                            nameRegexp:
                              description: |-
                                (String) Catalog name regexp the rule is applied to.
                                Property name regexp the rule is applied to.
                              type: string
                            names:
                              description: |-
                                (List of String) Property names rule is applied to.
                                Property names rule is applied to.
                              items:
                                type: string
                              type: array
                          type: object
                        users:
                          description: |-
                            (List of String) IAM user IDs the rule is applied to.
                            IAM user IDs the rule is applied to.
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  tables:
                    description: (Attributes List) Table level access control rules.
                      (see below for nested schema)
                    items:
                      properties:
                        catalog:
                          description: (Attributes) Catalog matcher specifying what
                            catalogs the rule is applied to. Exactly one of name_regexp,
                            ids attributes should be set. (see below for nested schema)
                          properties:
                            ids:
                              description: |-
                                (List of String) Catalog IDs rule is applied to.
                                Catalog IDs rule is applied to.
                              items:
                                type: string
                              type: array
                            nameRegexp:
                              description: |-
                                (String) Catalog name regexp the rule is applied to.
                                Catalog name regexp the rule is applied to.
                              type: string
                          type: object
                        columns:
                          description: (Attributes List) Column rules. (see below
                            for nested schema)
                          items:
                            properties:
                              access:
                                description: |-
                                  (String) Column access mode. Valid values: NONE, ALL
                                  Column access mode. Valid values: `NONE`, `ALL`
                                type: string
                              mask:
                                description: |-
                                  (String) SQL expression mask to evaluate instead of original column values.
                                  SQL expression mask to evaluate instead of original column values.
                                type: string
                              name:
                                description: |-
                                  (String) Column name.
                                  Column name.
                                type: string
                            type: object
                          type: array
                        description:
                          description: |-
                            (String) Rule description.
                            Rule description.
                          type: string
                        filter:
                          description: |-
                            (String) Boolean SQL expression to filter table rows for particular user.
                            Boolean SQL expression to filter table rows for particular user.
                          type: string
                        groups:
                          description: |-
                            (List of String) IAM group IDs the rule is applied to.
                            IAM group IDs the rule is applied to.
                          items:
                            type: string
                          type: array
                        privileges:
                          description: |-
                            (List of String) Privileges granted by the rule. Valid values: EXECUTE, GRANT_EXECUTE, OWNERSHIP.
                            Privileges granted by the rule. Valid values: `SELECT`, `INSERT`, `DELETE`, `UPDATE`, `OWNERSHIP`, `GRANT_SELECT`.
                          items:
                            type: string
                          type: array
                        schema:
                          description: (Attributes) Matcher specifying what Schemas
                            the rule is applied to. Exactly one of name_regexp, names
                            attributes should be set. (see below for nested schema)
                          properties:
                            nameRegexp:
                              description: |-
                                (String) Catalog name regexp the rule is applied to.
                                Schema name regexp the rule is applied to.
                              type: string
                            names:
                              description: |-
                                (List of String) Property names rule is applied to.
                                Schema names rule is applied to.
                              items:
                                type: string
                              type: array
                          type: object
                        table:
                          description: (Attributes) Matcher specifying what tables
                            the rule is applied to. Exactly one of name_regexp, names
                            attributes should be set. (see below for nested schema)
                          properties:
                            nameRegexp:
                              description: |-
                                (String) Catalog name regexp the rule is applied to.
                                Table name regexp the rule is applied to.
                              type: string
                            names:
                              description: |-
                                (List of String) Property names rule is applied to.
                                Table names rule is applied to.
                              items:
                                type: string
                              type: array
                          type: object
                        users:
                          description: |-
                            (List of String) IAM user IDs the rule is applied to.
                            IAM user IDs the rule is applied to.
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
