---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.13.0
  name: greenplumclusters.mdb.yandex-cloud.upjet.crossplane.io
spec:
  group: mdb.yandex-cloud.upjet.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - yandex-cloud
    kind: GreenplumCluster
    listKind: GreenplumClusterList
    plural: greenplumclusters
    singular: greenplumcluster
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: GreenplumCluster is the Schema for the GreenplumClusters API.
          Manages a Greenplum cluster within Yandex.Cloud.
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: GreenplumClusterSpec defines the desired state of GreenplumCluster
            properties:
              deletionPolicy:
                default: Delete
                description: 'DeletionPolicy specifies what will happen to the underlying
                  external when this managed resource is deleted - either "Delete"
                  or "Orphan" the external resource. This field is planned to be deprecated
                  in favor of the ManagementPolicies field in a future release. Currently,
                  both could be set independently and non-default values would be
                  honored if the feature flag is enabled. See the design doc for more
                  information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223'
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  access:
                    description: Access policy to the Greenplum cluster. The structure
                      is documented below.
                    items:
                      properties:
                        dataLens:
                          description: Allow access for Yandex DataLens.
                          type: boolean
                        dataTransfer:
                          description: Allow access for DataTransfer
                          type: boolean
                        webSql:
                          description: Allows access for SQL queries in the management
                            console.
                          type: boolean
                      type: object
                    type: array
                  assignPublicIp:
                    description: Sets whether the master hosts should get a public
                      IP address on creation. Changing this parameter for an existing
                      host is not supported at the moment.
                    type: boolean
                  backupWindowStart:
                    description: Time to start the daily backup, in the UTC timezone.
                      The structure is documented below.
                    items:
                      properties:
                        hours:
                          description: The hour at which backup will be started (UTC).
                          type: number
                        minutes:
                          description: The minute at which backup will be started
                            (UTC).
                          type: number
                      type: object
                    type: array
                  cloudStorage:
                    description: Cloud Storage settings of the Greenplum cluster.
                      The structure is documented below.
                    items:
                      properties:
                        enable:
                          description: Whether to use cloud storage or not.
                          type: boolean
                      type: object
                    type: array
                  deletionProtection:
                    description: Inhibits deletion of the cluster.  Can be either
                      true or false.
                    type: boolean
                  description:
                    description: Description of the Greenplum cluster.
                    type: string
                  environment:
                    description: Deployment environment of the Greenplum cluster.
                      (PRODUCTION, PRESTABLE)
                    type: string
                  folderId:
                    description: The ID of the folder that the resource belongs to.
                      If it is not provided, the default provider folder is used.
                    type: string
                  folderIdRef:
                    description: Reference to a Folder in resourcemanager to populate
                      folderId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: Resolution specifies whether resolution of
                              this reference is required. The default is 'Required',
                              which means the reconcile will fail if the reference
                              cannot be resolved. 'Optional' means this reference
                              will be a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: Resolve specifies when this reference should
                              be resolved. The default is 'IfNotPresent', which will
                              attempt to resolve the reference only when the corresponding
                              field is not present. Use 'Always' to resolve the reference
                              on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  folderIdSelector:
                    description: Selector for a Folder in resourcemanager to populate
                      folderId.
                    properties:
                      matchControllerRef:
                        description: MatchControllerRef ensures an object with the
                          same controller reference as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: Resolution specifies whether resolution of
                              this reference is required. The default is 'Required',
                              which means the reconcile will fail if the reference
                              cannot be resolved. 'Optional' means this reference
                              will be a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: Resolve specifies when this reference should
                              be resolved. The default is 'IfNotPresent', which will
                              attempt to resolve the reference only when the corresponding
                              field is not present. Use 'Always' to resolve the reference
                              on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  greenplumConfig:
                    additionalProperties:
                      type: string
                    description: Greenplum cluster config. Detail info in "Greenplum
                      cluster settings" section (documented below).
                    type: object
                  labels:
                    additionalProperties:
                      type: string
                    description: A set of key/value label pairs to assign to the Greenplum
                      cluster.
                    type: object
                  maintenanceWindow:
                    description: Maintenance policy of the Greenplum cluster. The
                      structure is documented below.
                    items:
                      properties:
                        day:
                          description: 'Day of the week (in DDD format). Allowed values:
                            "MON", "TUE", "WED", "THU", "FRI", "SAT", "SUN"'
                          type: string
                        hour:
                          description: Hour of the day in UTC (in HH format). Allowed
                            value is between 0 and 23.
                          type: number
                        type:
                          description: Type of maintenance window. Can be either ANYTIME
                            or WEEKLY. A day and hour of window need to be specified
                            with weekly window.
                          type: string
                      type: object
                    type: array
                  masterHostCount:
                    description: Number of hosts in master subcluster (1 or 2).
                    type: number
                  masterSubcluster:
                    description: Settings for master subcluster. The structure is
                      documented below.
                    items:
                      properties:
                        resources:
                          description: Resources allocated to hosts for master subcluster
                            of the Greenplum cluster. The structure is documented
                            below.
                          items:
                            properties:
                              diskSize:
                                type: number
                              diskTypeId:
                                type: string
                              resourcePresetId:
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  name:
                    description: Name of the Greenplum cluster. Provided by the client
                      when the cluster is created.
                    type: string
                  networkId:
                    description: ID of the network, to which the Greenplum cluster
                      uses.
                    type: string
                  networkIdRef:
                    description: Reference to a Network in vpc to populate networkId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: Resolution specifies whether resolution of
                              this reference is required. The default is 'Required',
                              which means the reconcile will fail if the reference
                              cannot be resolved. 'Optional' means this reference
                              will be a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: Resolve specifies when this reference should
                              be resolved. The default is 'IfNotPresent', which will
                              attempt to resolve the reference only when the corresponding
                              field is not present. Use 'Always' to resolve the reference
                              on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  networkIdSelector:
                    description: Selector for a Network in vpc to populate networkId.
                    properties:
                      matchControllerRef:
                        description: MatchControllerRef ensures an object with the
                          same controller reference as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: Resolution specifies whether resolution of
                              this reference is required. The default is 'Required',
                              which means the reconcile will fail if the reference
                              cannot be resolved. 'Optional' means this reference
                              will be a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: Resolve specifies when this reference should
                              be resolved. The default is 'IfNotPresent', which will
                              attempt to resolve the reference only when the corresponding
                              field is not present. Use 'Always' to resolve the reference
                              on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  poolerConfig:
                    description: Configuration of the connection pooler. The structure
                      is documented below.
                    items:
                      properties:
                        poolClientIdleTimeout:
                          description: Value for pool_client_idle_timeout parameter
                            in Odyssey.
                          type: number
                        poolSize:
                          description: Value for pool_size parameter in Odyssey.
                          type: number
                        poolingMode:
                          description: Mode that the connection pooler is working
                            in. See descriptions of all modes in the [documentation
                            for Odyssey](https://github.com/yandex/odyssey/blob/master/documentation/configuration.md#pool-string.
                          type: string
                      type: object
                    type: array
                  securityGroupIds:
                    description: A set of ids of security groups assigned to hosts
                      of the cluster.
                    items:
                      type: string
                    type: array
                  securityGroupIdsRefs:
                    description: References to SecurityGroup in vpc to populate securityGroupIds.
                    items:
                      description: A Reference to a named object.
                      properties:
                        name:
                          description: Name of the referenced object.
                          type: string
                        policy:
                          description: Policies for referencing.
                          properties:
                            resolution:
                              default: Required
                              description: Resolution specifies whether resolution
                                of this reference is required. The default is 'Required',
                                which means the reconcile will fail if the reference
                                cannot be resolved. 'Optional' means this reference
                                will be a no-op if it cannot be resolved.
                              enum:
                              - Required
                              - Optional
                              type: string
                            resolve:
                              description: Resolve specifies when this reference should
                                be resolved. The default is 'IfNotPresent', which
                                will attempt to resolve the reference only when the
                                corresponding field is not present. Use 'Always' to
                                resolve the reference on every reconcile.
                              enum:
                              - Always
                              - IfNotPresent
                              type: string
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                  securityGroupIdsSelector:
                    description: Selector for a list of SecurityGroup in vpc to populate
                      securityGroupIds.
                    properties:
                      matchControllerRef:
                        description: MatchControllerRef ensures an object with the
                          same controller reference as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: Resolution specifies whether resolution of
                              this reference is required. The default is 'Required',
                              which means the reconcile will fail if the reference
                              cannot be resolved. 'Optional' means this reference
                              will be a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: Resolve specifies when this reference should
                              be resolved. The default is 'IfNotPresent', which will
                              attempt to resolve the reference only when the corresponding
                              field is not present. Use 'Always' to resolve the reference
                              on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  segmentHostCount:
                    description: Number of hosts in segment subcluster (from 1 to
                      32).
                    type: number
                  segmentInHost:
                    description: Number of segments on segment host (not more then
                      1 + RAM/8).
                    type: number
                  segmentSubcluster:
                    description: Settings for segment subcluster. The structure is
                      documented below.
                    items:
                      properties:
                        resources:
                          description: Resources allocated to hosts for segment subcluster
                            of the Greenplum cluster. The structure is documented
                            below.
                          items:
                            properties:
                              diskSize:
                                type: number
                              diskTypeId:
                                type: string
                              resourcePresetId:
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  subnetId:
                    description: The ID of the subnet, to which the hosts belongs.
                      The subnet must be a part of the network to which the cluster
                      belongs.
                    type: string
                  subnetIdRef:
                    description: Reference to a Subnet in vpc to populate subnetId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: Resolution specifies whether resolution of
                              this reference is required. The default is 'Required',
                              which means the reconcile will fail if the reference
                              cannot be resolved. 'Optional' means this reference
                              will be a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: Resolve specifies when this reference should
                              be resolved. The default is 'IfNotPresent', which will
                              attempt to resolve the reference only when the corresponding
                              field is not present. Use 'Always' to resolve the reference
                              on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  subnetIdSelector:
                    description: Selector for a Subnet in vpc to populate subnetId.
                    properties:
                      matchControllerRef:
                        description: MatchControllerRef ensures an object with the
                          same controller reference as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: Resolution specifies whether resolution of
                              this reference is required. The default is 'Required',
                              which means the reconcile will fail if the reference
                              cannot be resolved. 'Optional' means this reference
                              will be a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: Resolve specifies when this reference should
                              be resolved. The default is 'IfNotPresent', which will
                              attempt to resolve the reference only when the corresponding
                              field is not present. Use 'Always' to resolve the reference
                              on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  userName:
                    description: Greenplum cluster admin user name.
                    type: string
                  userPasswordSecretRef:
                    description: Greenplum cluster admin password name.
                    properties:
                      key:
                        description: The key to select.
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                      namespace:
                        description: Namespace of the secret.
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                  version:
                    description: Version of the Greenplum cluster. (6.22 or 6.25)
                    type: string
                  zone:
                    description: The availability zone where the Greenplum hosts will
                      be created.
                    type: string
                type: object
              initProvider:
                description: THIS IS AN ALPHA FIELD. Do not use it in production.
                  It is not honored unless the relevant Crossplane feature flag is
                  enabled, and may be changed or removed without notice. InitProvider
                  holds the same fields as ForProvider, with the exception of Identifier
                  and other resource reference fields. The fields that are in InitProvider
                  are merged into ForProvider when the resource is created. The same
                  fields are also added to the terraform ignore_changes hook, to avoid
                  updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after
                  creation, for example because of an external controller is managing
                  them, like an autoscaler.
                properties:
                  access:
                    description: Access policy to the Greenplum cluster. The structure
                      is documented below.
                    items:
                      properties:
                        dataLens:
                          description: Allow access for Yandex DataLens.
                          type: boolean
                        dataTransfer:
                          description: Allow access for DataTransfer
                          type: boolean
                        webSql:
                          description: Allows access for SQL queries in the management
                            console.
                          type: boolean
                      type: object
                    type: array
                  assignPublicIp:
                    description: Sets whether the master hosts should get a public
                      IP address on creation. Changing this parameter for an existing
                      host is not supported at the moment.
                    type: boolean
                  backupWindowStart:
                    description: Time to start the daily backup, in the UTC timezone.
                      The structure is documented below.
                    items:
                      properties:
                        hours:
                          description: The hour at which backup will be started (UTC).
                          type: number
                        minutes:
                          description: The minute at which backup will be started
                            (UTC).
                          type: number
                      type: object
                    type: array
                  cloudStorage:
                    description: Cloud Storage settings of the Greenplum cluster.
                      The structure is documented below.
                    items:
                      properties:
                        enable:
                          description: Whether to use cloud storage or not.
                          type: boolean
                      type: object
                    type: array
                  deletionProtection:
                    description: Inhibits deletion of the cluster.  Can be either
                      true or false.
                    type: boolean
                  description:
                    description: Description of the Greenplum cluster.
                    type: string
                  environment:
                    description: Deployment environment of the Greenplum cluster.
                      (PRODUCTION, PRESTABLE)
                    type: string
                  greenplumConfig:
                    additionalProperties:
                      type: string
                    description: Greenplum cluster config. Detail info in "Greenplum
                      cluster settings" section (documented below).
                    type: object
                  labels:
                    additionalProperties:
                      type: string
                    description: A set of key/value label pairs to assign to the Greenplum
                      cluster.
                    type: object
                  maintenanceWindow:
                    description: Maintenance policy of the Greenplum cluster. The
                      structure is documented below.
                    items:
                      properties:
                        day:
                          description: 'Day of the week (in DDD format). Allowed values:
                            "MON", "TUE", "WED", "THU", "FRI", "SAT", "SUN"'
                          type: string
                        hour:
                          description: Hour of the day in UTC (in HH format). Allowed
                            value is between 0 and 23.
                          type: number
                        type:
                          description: Type of maintenance window. Can be either ANYTIME
                            or WEEKLY. A day and hour of window need to be specified
                            with weekly window.
                          type: string
                      type: object
                    type: array
                  masterHostCount:
                    description: Number of hosts in master subcluster (1 or 2).
                    type: number
                  masterSubcluster:
                    description: Settings for master subcluster. The structure is
                      documented below.
                    items:
                      properties:
                        resources:
                          description: Resources allocated to hosts for master subcluster
                            of the Greenplum cluster. The structure is documented
                            below.
                          items:
                            properties:
                              diskSize:
                                type: number
                              diskTypeId:
                                type: string
                              resourcePresetId:
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  name:
                    description: Name of the Greenplum cluster. Provided by the client
                      when the cluster is created.
                    type: string
                  poolerConfig:
                    description: Configuration of the connection pooler. The structure
                      is documented below.
                    items:
                      properties:
                        poolClientIdleTimeout:
                          description: Value for pool_client_idle_timeout parameter
                            in Odyssey.
                          type: number
                        poolSize:
                          description: Value for pool_size parameter in Odyssey.
                          type: number
                        poolingMode:
                          description: Mode that the connection pooler is working
                            in. See descriptions of all modes in the [documentation
                            for Odyssey](https://github.com/yandex/odyssey/blob/master/documentation/configuration.md#pool-string.
                          type: string
                      type: object
                    type: array
                  segmentHostCount:
                    description: Number of hosts in segment subcluster (from 1 to
                      32).
                    type: number
                  segmentInHost:
                    description: Number of segments on segment host (not more then
                      1 + RAM/8).
                    type: number
                  segmentSubcluster:
                    description: Settings for segment subcluster. The structure is
                      documented below.
                    items:
                      properties:
                        resources:
                          description: Resources allocated to hosts for segment subcluster
                            of the Greenplum cluster. The structure is documented
                            below.
                          items:
                            properties:
                              diskSize:
                                type: number
                              diskTypeId:
                                type: string
                              resourcePresetId:
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  userName:
                    description: Greenplum cluster admin user name.
                    type: string
                  version:
                    description: Version of the Greenplum cluster. (6.22 or 6.25)
                    type: string
                  zone:
                    description: The availability zone where the Greenplum hosts will
                      be created.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: 'THIS IS AN ALPHA FIELD. Do not use it in production.
                  It is not honored unless the relevant Crossplane feature flag is
                  enabled, and may be changed or removed without notice. ManagementPolicies
                  specify the array of actions Crossplane is allowed to take on the
                  managed and external resources. This field is planned to replace
                  the DeletionPolicy field in a future release. Currently, both could
                  be set independently and non-default values would be honored if
                  the feature flag is enabled. If both are custom, the DeletionPolicy
                  field will be ignored. See the design doc for more information:
                  https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md'
                items:
                  description: A ManagementAction represents an action that the Crossplane
                    controllers can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: ProviderConfigReference specifies how the provider that
                  will be used to create, observe, update, and delete this managed
                  resource should be configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: Resolution specifies whether resolution of this
                          reference is required. The default is 'Required', which
                          means the reconcile will fail if the reference cannot be
                          resolved. 'Optional' means this reference will be a no-op
                          if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: Resolve specifies when this reference should
                          be resolved. The default is 'IfNotPresent', which will attempt
                          to resolve the reference only when the corresponding field
                          is not present. Use 'Always' to resolve the reference on
                          every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              publishConnectionDetailsTo:
                description: PublishConnectionDetailsTo specifies the connection secret
                  config which contains a name, metadata and a reference to secret
                  store config to which any connection details for this managed resource
                  should be written. Connection details frequently include the endpoint,
                  username, and password required to connect to the managed resource.
                properties:
                  configRef:
                    default:
                      name: default
                    description: SecretStoreConfigRef specifies which secret store
                      config should be used for this ConnectionSecret.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: Resolution specifies whether resolution of
                              this reference is required. The default is 'Required',
                              which means the reconcile will fail if the reference
                              cannot be resolved. 'Optional' means this reference
                              will be a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: Resolve specifies when this reference should
                              be resolved. The default is 'IfNotPresent', which will
                              attempt to resolve the reference only when the corresponding
                              field is not present. Use 'Always' to resolve the reference
                              on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  metadata:
                    description: Metadata is the metadata for connection secret.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: Annotations are the annotations to be added to
                          connection secret. - For Kubernetes secrets, this will be
                          used as "metadata.annotations". - It is up to Secret Store
                          implementation for others store types.
                        type: object
                      labels:
                        additionalProperties:
                          type: string
                        description: Labels are the labels/tags to be added to connection
                          secret. - For Kubernetes secrets, this will be used as "metadata.labels".
                          - It is up to Secret Store implementation for others store
                          types.
                        type: object
                      type:
                        description: Type is the SecretType for the connection secret.
                          - Only valid for Kubernetes Secret Stores.
                        type: string
                    type: object
                  name:
                    description: Name is the name of the connection secret.
                    type: string
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: WriteConnectionSecretToReference specifies the namespace
                  and name of a Secret to which any connection details for this managed
                  resource should be written. Connection details frequently include
                  the endpoint, username, and password required to connect to the
                  managed resource. This field is planned to be replaced in a future
                  release in favor of PublishConnectionDetailsTo. Currently, both
                  could be set independently and connection details would be published
                  to both without affecting each other.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.assignPublicIp is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.assignPublicIp)
                || (has(self.initProvider) && has(self.initProvider.assignPublicIp))'
            - message: spec.forProvider.environment is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.environment)
                || (has(self.initProvider) && has(self.initProvider.environment))'
            - message: spec.forProvider.masterHostCount is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.masterHostCount)
                || (has(self.initProvider) && has(self.initProvider.masterHostCount))'
            - message: spec.forProvider.masterSubcluster is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.masterSubcluster)
                || (has(self.initProvider) && has(self.initProvider.masterSubcluster))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.segmentHostCount is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.segmentHostCount)
                || (has(self.initProvider) && has(self.initProvider.segmentHostCount))'
            - message: spec.forProvider.segmentInHost is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.segmentInHost)
                || (has(self.initProvider) && has(self.initProvider.segmentInHost))'
            - message: spec.forProvider.segmentSubcluster is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.segmentSubcluster)
                || (has(self.initProvider) && has(self.initProvider.segmentSubcluster))'
            - message: spec.forProvider.userName is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.userName)
                || (has(self.initProvider) && has(self.initProvider.userName))'
            - message: spec.forProvider.userPasswordSecretRef is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.userPasswordSecretRef)'
            - message: spec.forProvider.version is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.version)
                || (has(self.initProvider) && has(self.initProvider.version))'
            - message: spec.forProvider.zone is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.zone)
                || (has(self.initProvider) && has(self.initProvider.zone))'
          status:
            description: GreenplumClusterStatus defines the observed state of GreenplumCluster.
            properties:
              atProvider:
                properties:
                  access:
                    description: Access policy to the Greenplum cluster. The structure
                      is documented below.
                    items:
                      properties:
                        dataLens:
                          description: Allow access for Yandex DataLens.
                          type: boolean
                        dataTransfer:
                          description: Allow access for DataTransfer
                          type: boolean
                        webSql:
                          description: Allows access for SQL queries in the management
                            console.
                          type: boolean
                      type: object
                    type: array
                  assignPublicIp:
                    description: Sets whether the master hosts should get a public
                      IP address on creation. Changing this parameter for an existing
                      host is not supported at the moment.
                    type: boolean
                  backupWindowStart:
                    description: Time to start the daily backup, in the UTC timezone.
                      The structure is documented below.
                    items:
                      properties:
                        hours:
                          description: The hour at which backup will be started (UTC).
                          type: number
                        minutes:
                          description: The minute at which backup will be started
                            (UTC).
                          type: number
                      type: object
                    type: array
                  cloudStorage:
                    description: Cloud Storage settings of the Greenplum cluster.
                      The structure is documented below.
                    items:
                      properties:
                        enable:
                          description: Whether to use cloud storage or not.
                          type: boolean
                      type: object
                    type: array
                  createdAt:
                    description: Creation timestamp of the cluster.
                    type: string
                  deletionProtection:
                    description: Inhibits deletion of the cluster.  Can be either
                      true or false.
                    type: boolean
                  description:
                    description: Description of the Greenplum cluster.
                    type: string
                  environment:
                    description: Deployment environment of the Greenplum cluster.
                      (PRODUCTION, PRESTABLE)
                    type: string
                  folderId:
                    description: The ID of the folder that the resource belongs to.
                      If it is not provided, the default provider folder is used.
                    type: string
                  greenplumConfig:
                    additionalProperties:
                      type: string
                    description: Greenplum cluster config. Detail info in "Greenplum
                      cluster settings" section (documented below).
                    type: object
                  health:
                    description: Aggregated health of the cluster.
                    type: string
                  id:
                    type: string
                  labels:
                    additionalProperties:
                      type: string
                    description: A set of key/value label pairs to assign to the Greenplum
                      cluster.
                    type: object
                  maintenanceWindow:
                    description: Maintenance policy of the Greenplum cluster. The
                      structure is documented below.
                    items:
                      properties:
                        day:
                          description: 'Day of the week (in DDD format). Allowed values:
                            "MON", "TUE", "WED", "THU", "FRI", "SAT", "SUN"'
                          type: string
                        hour:
                          description: Hour of the day in UTC (in HH format). Allowed
                            value is between 0 and 23.
                          type: number
                        type:
                          description: Type of maintenance window. Can be either ANYTIME
                            or WEEKLY. A day and hour of window need to be specified
                            with weekly window.
                          type: string
                      type: object
                    type: array
                  masterHostCount:
                    description: Number of hosts in master subcluster (1 or 2).
                    type: number
                  masterHosts:
                    description: (Computed) Info about hosts in master subcluster.
                      The structure is documented below.
                    items:
                      properties:
                        assignPublicIp:
                          description: (Computed) Flag indicating that master hosts
                            should be created with a public IP address.
                          type: boolean
                        fqdn:
                          description: (Computed) The fully qualified domain name
                            of the host.
                          type: string
                      type: object
                    type: array
                  masterSubcluster:
                    description: Settings for master subcluster. The structure is
                      documented below.
                    items:
                      properties:
                        resources:
                          description: Resources allocated to hosts for master subcluster
                            of the Greenplum cluster. The structure is documented
                            below.
                          items:
                            properties:
                              diskSize:
                                type: number
                              diskTypeId:
                                type: string
                              resourcePresetId:
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  name:
                    description: Name of the Greenplum cluster. Provided by the client
                      when the cluster is created.
                    type: string
                  networkId:
                    description: ID of the network, to which the Greenplum cluster
                      uses.
                    type: string
                  poolerConfig:
                    description: Configuration of the connection pooler. The structure
                      is documented below.
                    items:
                      properties:
                        poolClientIdleTimeout:
                          description: Value for pool_client_idle_timeout parameter
                            in Odyssey.
                          type: number
                        poolSize:
                          description: Value for pool_size parameter in Odyssey.
                          type: number
                        poolingMode:
                          description: Mode that the connection pooler is working
                            in. See descriptions of all modes in the [documentation
                            for Odyssey](https://github.com/yandex/odyssey/blob/master/documentation/configuration.md#pool-string.
                          type: string
                      type: object
                    type: array
                  securityGroupIds:
                    description: A set of ids of security groups assigned to hosts
                      of the cluster.
                    items:
                      type: string
                    type: array
                  segmentHostCount:
                    description: Number of hosts in segment subcluster (from 1 to
                      32).
                    type: number
                  segmentHosts:
                    description: (Computed) Info about hosts in segment subcluster.
                      The structure is documented below.
                    items:
                      properties:
                        fqdn:
                          description: (Computed) The fully qualified domain name
                            of the host.
                          type: string
                      type: object
                    type: array
                  segmentInHost:
                    description: Number of segments on segment host (not more then
                      1 + RAM/8).
                    type: number
                  segmentSubcluster:
                    description: Settings for segment subcluster. The structure is
                      documented below.
                    items:
                      properties:
                        resources:
                          description: Resources allocated to hosts for segment subcluster
                            of the Greenplum cluster. The structure is documented
                            below.
                          items:
                            properties:
                              diskSize:
                                type: number
                              diskTypeId:
                                type: string
                              resourcePresetId:
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  status:
                    description: Status of the cluster.
                    type: string
                  subnetId:
                    description: The ID of the subnet, to which the hosts belongs.
                      The subnet must be a part of the network to which the cluster
                      belongs.
                    type: string
                  userName:
                    description: Greenplum cluster admin user name.
                    type: string
                  version:
                    description: Version of the Greenplum cluster. (6.22 or 6.25)
                    type: string
                  zone:
                    description: The availability zone where the Greenplum hosts will
                      be created.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: LastTransitionTime is the last time this condition
                        transitioned from one status to another.
                      format: date-time
                      type: string
                    message:
                      description: A Message containing details about this condition's
                        last transition from one status to another, if any.
                      type: string
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: Type of this condition. At most one of each condition
                        type may apply to a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
