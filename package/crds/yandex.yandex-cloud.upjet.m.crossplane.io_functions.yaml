---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: functions.yandex.yandex-cloud.upjet.m.crossplane.io
spec:
  group: yandex.yandex-cloud.upjet.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - yandex-cloud
    kind: Function
    listKind: FunctionList
    plural: functions
    singular: function
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Function is the Schema for the Functions API. Allows management
          of a Yandex Cloud Function.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: FunctionSpec defines the desired state of Function
            properties:
              forProvider:
                properties:
                  asyncInvocation:
                    description: |-
                      (Block List, Max: 1) Config for asynchronous invocations of Yandex Cloud Function. (see below for nested schema)
                      Config for asynchronous invocations of Yandex Cloud Function.
                    items:
                      properties:
                        retriesCount:
                          description: |-
                            (Number) Maximum number of retries for async invocation.
                            Maximum number of retries for async invocation.
                          type: number
                        serviceAccountId:
                          description: |-
                            (String) Service account which linked to the resource.
                            Service account used for async invocation.
                          type: string
                        ymqFailureTarget:
                          description: |-
                            (Block List, Max: 1) Target for unsuccessful async invocation. (see below for nested schema)
                            Target for unsuccessful async invocation.
                          items:
                            properties:
                              arn:
                                description: |-
                                  (String) YMQ ARN.
                                  YMQ ARN.
                                type: string
                              serviceAccountId:
                                description: |-
                                  (String) Service account which linked to the resource.
                                  Service account used for writing result to queue.
                                type: string
                            type: object
                          type: array
                        ymqSuccessTarget:
                          description: |-
                            (Block List, Max: 1) Target for successful async invocation. (see below for nested schema)
                            Target for successful async invocation.
                          items:
                            properties:
                              arn:
                                description: |-
                                  (String) YMQ ARN.
                                  YMQ ARN.
                                type: string
                              serviceAccountId:
                                description: |-
                                  (String) Service account which linked to the resource.
                                  Service account used for writing result to queue.
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  concurrency:
                    description: |-
                      (Number) The maximum number of requests processed by a function instance at the same time.
                      The maximum number of requests processed by a function instance at the same time.
                    type: number
                  connectivity:
                    description: |-
                      (Block List, Max: 1) Function version connectivity. If specified the version will be attached to specified network. (see below for nested schema)
                      Function version connectivity. If specified the version will be attached to specified network.
                    items:
                      properties:
                        networkId:
                          description: |-
                            (String) Network the version will have access to. It's essential to specify network with subnets in all availability zones.
                            Network the version will have access to. It's essential to specify network with subnets in all availability zones.
                          type: string
                      type: object
                    type: array
                  content:
                    description: |-
                      (Block List, Max: 1) Version deployment content for Yandex Cloud Function code. Can be only one package or content section. Either package or content section must be specified. (see below for nested schema)
                      Version deployment content for Yandex Cloud Function code. Can be only one `package` or `content` section. Either `package` or `content` section must be specified.
                    items:
                      properties:
                        zipFilename:
                          description: |-
                            (String) Filename to zip archive for the version.
                            Filename to zip archive for the version.
                          type: string
                      type: object
                    type: array
                  description:
                    description: |-
                      (String) The resource description.
                      The resource description.
                    type: string
                  entrypoint:
                    description: |-
                      (String) Entrypoint for Yandex Cloud Function.
                      Entrypoint for Yandex Cloud Function.
                    type: string
                  environment:
                    additionalProperties:
                      type: string
                    description: |-
                      Z, a-z).
                      A set of key/value environment variables for Yandex Cloud Function. Each key must begin with a letter (A-Z, a-z).
                    type: object
                    x-kubernetes-map-type: granular
                  executionTimeout:
                    description: |-
                      (String) Execution timeout in seconds for Yandex Cloud Function.
                      Execution timeout in seconds for Yandex Cloud Function.
                    type: string
                  folderId:
                    description: |-
                      id is used.
                      The folder identifier that resource belongs to. If it is not provided, the default provider `folder-id` is used.
                    type: string
                  folderIdRef:
                    description: Reference to a Folder in resourcemanager to populate
                      folderId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  folderIdSelector:
                    description: Selector for a Folder in resourcemanager to populate
                      folderId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  labels:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) A set of key/value label pairs which assigned to resource.
                      A set of key/value label pairs which assigned to resource.
                    type: object
                    x-kubernetes-map-type: granular
                  logOptions:
                    description: |-
                      (Block List, Max: 1) Options for logging from Yandex Cloud Function. (see below for nested schema)
                      Options for logging from Yandex Cloud Function.
                    items:
                      properties:
                        disabled:
                          description: |-
                            (Boolean) Is logging from function disabled.
                            Is logging from function disabled.
                          type: boolean
                        folderId:
                          description: |-
                            id is used.
                            Log entries are written to default log group for specified folder.
                          type: string
                        logGroupId:
                          description: |-
                            (String) Log entries are written to specified log group.
                            Log entries are written to specified log group.
                          type: string
                        minLevel:
                          description: |-
                            (String) Minimum log entry level.
                            Minimum log entry level.
                          type: string
                      type: object
                    type: array
                  memory:
                    description: |-
                      (Number) Memory in megabytes (aligned to 128MB) for Yandex Cloud Function.
                      Memory in megabytes (**aligned to 128MB**) for Yandex Cloud Function.
                    type: number
                  metadataOptions:
                    description: |-
                      (Block List, Max: 1) Options set the access mode to function's metadata endpoints. (see below for nested schema)
                      Options set the access mode to function's metadata endpoints.
                    items:
                      properties:
                        awsV1HttpEndpoint:
                          description: |-
                            default, 1 - enabled, 2 - disabled.
                            Enables access to AWS flavored metadata (IMDSv1). Values: `0` - default, `1` - enabled, `2` - disabled.
                          type: number
                        gceHttpEndpoint:
                          description: |-
                            default, 1 - enabled, 2 - disabled.
                            Enables access to GCE flavored metadata. Values: `0`- default, `1` - enabled, `2` - disabled.
                          type: number
                      type: object
                    type: array
                  mounts:
                    description: |-
                      (Block List) Mounts for Yandex Cloud Function. (see below for nested schema)
                      Mounts for Yandex Cloud Function.
                    items:
                      properties:
                        ephemeralDisk:
                          description: |-
                            (Block List, Max: 1) One of the available mount types. Disk available during the function execution time. (see below for nested schema)
                            One of the available mount types. Disk available during the function execution time.
                          items:
                            properties:
                              blockSizeKb:
                                description: |-
                                  (Number) Optional block size of the ephemeral disk in KB.
                                  Optional block size of the ephemeral disk in KB.
                                type: number
                              sizeGb:
                                description: |-
                                  (Number) Size of the ephemeral disk in GB.
                                  Size of the ephemeral disk in GB.
                                type: number
                            type: object
                          type: array
                        mode:
                          description: |-
                            (String) Mount’s accessibility mode. Valid values are ro and rw.
                            Mount’s accessibility mode. Valid values are `ro` and `rw`.
                          type: string
                        name:
                          description: |-
                            (String) The resource name.
                            Name of the mount point. The directory where the target is mounted will be accessible at the `/function/storage/<mounts.0.name>` path.
                          type: string
                        objectStorage:
                          description: |-
                            (Block List, Max: 1) One of the available mount types. Object storage as a mount. (see below for nested schema)
                            One of the available mount types. Object storage as a mount.
                          items:
                            properties:
                              bucket:
                                description: |-
                                  (String) Name of the mounting bucket.
                                  Name of the mounting bucket.
                                type: string
                              bucketRef:
                                description: Reference to a Bucket in storage to populate
                                  bucket.
                                properties:
                                  name:
                                    description: Name of the referenced object.
                                    type: string
                                  namespace:
                                    description: Namespace of the referenced object
                                    type: string
                                  policy:
                                    description: Policies for referencing.
                                    properties:
                                      resolution:
                                        default: Required
                                        description: |-
                                          Resolution specifies whether resolution of this reference is required.
                                          The default is 'Required', which means the reconcile will fail if the
                                          reference cannot be resolved. 'Optional' means this reference will be
                                          a no-op if it cannot be resolved.
                                        enum:
                                        - Required
                                        - Optional
                                        type: string
                                      resolve:
                                        description: |-
                                          Resolve specifies when this reference should be resolved. The default
                                          is 'IfNotPresent', which will attempt to resolve the reference only when
                                          the corresponding field is not present. Use 'Always' to resolve the
                                          reference on every reconcile.
                                        enum:
                                        - Always
                                        - IfNotPresent
                                        type: string
                                    type: object
                                required:
                                - name
                                type: object
                              bucketSelector:
                                description: Selector for a Bucket in storage to populate
                                  bucket.
                                properties:
                                  matchControllerRef:
                                    description: |-
                                      MatchControllerRef ensures an object with the same controller reference
                                      as the selecting object is selected.
                                    type: boolean
                                  matchLabels:
                                    additionalProperties:
                                      type: string
                                    description: MatchLabels ensures an object with
                                      matching labels is selected.
                                    type: object
                                  namespace:
                                    description: Namespace for the selector
                                    type: string
                                  policy:
                                    description: Policies for selection.
                                    properties:
                                      resolution:
                                        default: Required
                                        description: |-
                                          Resolution specifies whether resolution of this reference is required.
                                          The default is 'Required', which means the reconcile will fail if the
                                          reference cannot be resolved. 'Optional' means this reference will be
                                          a no-op if it cannot be resolved.
                                        enum:
                                        - Required
                                        - Optional
                                        type: string
                                      resolve:
                                        description: |-
                                          Resolve specifies when this reference should be resolved. The default
                                          is 'IfNotPresent', which will attempt to resolve the reference only when
                                          the corresponding field is not present. Use 'Always' to resolve the
                                          reference on every reconcile.
                                        enum:
                                        - Always
                                        - IfNotPresent
                                        type: string
                                    type: object
                                type: object
                              prefix:
                                description: |-
                                  (String) Prefix within the bucket. If you leave this field empty, the entire bucket will be mounted.
                                  Prefix within the bucket. If you leave this field empty, the entire bucket will be mounted.
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  name:
                    description: |-
                      (String) The resource name.
                      The resource name.
                    type: string
                  package:
                    description: |-
                      (Block List, Max: 1) Version deployment package for Yandex Cloud Function code. Can be only one package or content section. Either package or content section must be specified. (see below for nested schema)
                      Version deployment package for Yandex Cloud Function code. Can be only one `package` or `content` section. Either `package` or `content` section must be specified.
                    items:
                      properties:
                        bucketName:
                          description: |-
                            (String) Name of the bucket that stores the code for the version.
                            Name of the bucket that stores the code for the version.
                          type: string
                        objectName:
                          description: |-
                            (String) Name of the object in the bucket that stores the code for the version.
                            Name of the object in the bucket that stores the code for the version.
                          type: string
                        sha256:
                          description: |-
                            (String) SHA256 hash of the version deployment package.
                            SHA256 hash of the version deployment package.
                          type: string
                      type: object
                    type: array
                  runtime:
                    description: |-
                      (String) Runtime for Yandex Cloud Function.
                      Runtime for Yandex Cloud Function.
                    type: string
                  secrets:
                    description: |-
                      (Block List) Secrets for Yandex Cloud Function. (see below for nested schema)
                      Secrets for Yandex Cloud Function.
                    items:
                      properties:
                        environmentVariable:
                          description: |-
                            Z, a-z).
                            Function's environment variable in which secret's value will be stored. Must begin with a letter (A-Z, a-z).
                          type: string
                        id:
                          description: |-
                            (String) The ID of this resource.
                            Secret's ID.
                          type: string
                        idRef:
                          description: Reference to a Secret in lockbox to populate
                            id.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            namespace:
                              description: Namespace of the referenced object
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        idSelector:
                          description: Selector for a Secret in lockbox to populate
                            id.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            namespace:
                              description: Namespace for the selector
                              type: string
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                        key:
                          description: |-
                            (String) Secret's entries key which value will be stored in environment variable.
                            Secret's entries key which value will be stored in environment variable.
                          type: string
                        versionId:
                          description: |-
                            (String) Secret's version ID.
                            Secret's version ID.
                          type: string
                        versionIdRef:
                          description: Reference to a SecretVersion in lockbox to
                            populate versionId.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            namespace:
                              description: Namespace of the referenced object
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        versionIdSelector:
                          description: Selector for a SecretVersion in lockbox to
                            populate versionId.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            namespace:
                              description: Namespace for the selector
                              type: string
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                      type: object
                    type: array
                  serviceAccountId:
                    description: |-
                      (String) Service account which linked to the resource.
                      [Service account](https://yandex.cloud/docs/iam/concepts/users/service-accounts) which linked to the resource.
                    type: string
                  serviceAccountIdRef:
                    description: Reference to a ServiceAccount in iam to populate
                      serviceAccountId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  serviceAccountIdSelector:
                    description: Selector for a ServiceAccount in iam to populate
                      serviceAccountId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  storageMounts:
                    description: |-
                      > object_storage instead). Storage mounts for Yandex Cloud Function. (see below for nested schema)
                      (**DEPRECATED**, use `mounts -> object_storage` instead). Storage mounts for Yandex Cloud Function.
                    items:
                      properties:
                        bucket:
                          description: |-
                            (String) Name of the mounting bucket.
                            Name of the mounting bucket.
                          type: string
                        mountPointName:
                          description: |-
                            (String) Name of the mount point. The directory where the bucket is mounted will be accessible at the /function/storage/<mount_point> path.
                            Name of the mount point. The directory where the bucket is mounted will be accessible at the `/function/storage/<mount_point>` path.
                          type: string
                        prefix:
                          description: |-
                            (String) Prefix within the bucket. If you leave this field empty, the entire bucket will be mounted.
                            Prefix within the bucket. If you leave this field empty, the entire bucket will be mounted.
                          type: string
                        readOnly:
                          description: |-
                            only mode.
                            Mount the bucket in read-only mode.
                          type: boolean
                      type: object
                    type: array
                  tags:
                    description: |-
                      (Set of String) Tags for Yandex Cloud Function. Tag $latest isn't returned.
                      Tags for Yandex Cloud Function. Tag `$latest` isn't returned.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  tmpfsSize:
                    description: |-
                      (Number) Tmpfs size for Yandex Cloud Function.
                      Tmpfs size for Yandex Cloud Function.
                    type: number
                  userHash:
                    description: |-
                      defined string for current function version. User must change this string any times when function changed. Function will be updated when hash is changed.
                      User-defined string for current function version. User must change this string any times when function changed. Function will be updated when hash is changed.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  asyncInvocation:
                    description: |-
                      (Block List, Max: 1) Config for asynchronous invocations of Yandex Cloud Function. (see below for nested schema)
                      Config for asynchronous invocations of Yandex Cloud Function.
                    items:
                      properties:
                        retriesCount:
                          description: |-
                            (Number) Maximum number of retries for async invocation.
                            Maximum number of retries for async invocation.
                          type: number
                        serviceAccountId:
                          description: |-
                            (String) Service account which linked to the resource.
                            Service account used for async invocation.
                          type: string
                        ymqFailureTarget:
                          description: |-
                            (Block List, Max: 1) Target for unsuccessful async invocation. (see below for nested schema)
                            Target for unsuccessful async invocation.
                          items:
                            properties:
                              arn:
                                description: |-
                                  (String) YMQ ARN.
                                  YMQ ARN.
                                type: string
                              serviceAccountId:
                                description: |-
                                  (String) Service account which linked to the resource.
                                  Service account used for writing result to queue.
                                type: string
                            type: object
                          type: array
                        ymqSuccessTarget:
                          description: |-
                            (Block List, Max: 1) Target for successful async invocation. (see below for nested schema)
                            Target for successful async invocation.
                          items:
                            properties:
                              arn:
                                description: |-
                                  (String) YMQ ARN.
                                  YMQ ARN.
                                type: string
                              serviceAccountId:
                                description: |-
                                  (String) Service account which linked to the resource.
                                  Service account used for writing result to queue.
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  concurrency:
                    description: |-
                      (Number) The maximum number of requests processed by a function instance at the same time.
                      The maximum number of requests processed by a function instance at the same time.
                    type: number
                  connectivity:
                    description: |-
                      (Block List, Max: 1) Function version connectivity. If specified the version will be attached to specified network. (see below for nested schema)
                      Function version connectivity. If specified the version will be attached to specified network.
                    items:
                      properties:
                        networkId:
                          description: |-
                            (String) Network the version will have access to. It's essential to specify network with subnets in all availability zones.
                            Network the version will have access to. It's essential to specify network with subnets in all availability zones.
                          type: string
                      type: object
                    type: array
                  content:
                    description: |-
                      (Block List, Max: 1) Version deployment content for Yandex Cloud Function code. Can be only one package or content section. Either package or content section must be specified. (see below for nested schema)
                      Version deployment content for Yandex Cloud Function code. Can be only one `package` or `content` section. Either `package` or `content` section must be specified.
                    items:
                      properties:
                        zipFilename:
                          description: |-
                            (String) Filename to zip archive for the version.
                            Filename to zip archive for the version.
                          type: string
                      type: object
                    type: array
                  description:
                    description: |-
                      (String) The resource description.
                      The resource description.
                    type: string
                  entrypoint:
                    description: |-
                      (String) Entrypoint for Yandex Cloud Function.
                      Entrypoint for Yandex Cloud Function.
                    type: string
                  environment:
                    additionalProperties:
                      type: string
                    description: |-
                      Z, a-z).
                      A set of key/value environment variables for Yandex Cloud Function. Each key must begin with a letter (A-Z, a-z).
                    type: object
                    x-kubernetes-map-type: granular
                  executionTimeout:
                    description: |-
                      (String) Execution timeout in seconds for Yandex Cloud Function.
                      Execution timeout in seconds for Yandex Cloud Function.
                    type: string
                  folderId:
                    description: |-
                      id is used.
                      The folder identifier that resource belongs to. If it is not provided, the default provider `folder-id` is used.
                    type: string
                  folderIdRef:
                    description: Reference to a Folder in resourcemanager to populate
                      folderId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  folderIdSelector:
                    description: Selector for a Folder in resourcemanager to populate
                      folderId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  labels:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) A set of key/value label pairs which assigned to resource.
                      A set of key/value label pairs which assigned to resource.
                    type: object
                    x-kubernetes-map-type: granular
                  logOptions:
                    description: |-
                      (Block List, Max: 1) Options for logging from Yandex Cloud Function. (see below for nested schema)
                      Options for logging from Yandex Cloud Function.
                    items:
                      properties:
                        disabled:
                          description: |-
                            (Boolean) Is logging from function disabled.
                            Is logging from function disabled.
                          type: boolean
                        folderId:
                          description: |-
                            id is used.
                            Log entries are written to default log group for specified folder.
                          type: string
                        logGroupId:
                          description: |-
                            (String) Log entries are written to specified log group.
                            Log entries are written to specified log group.
                          type: string
                        minLevel:
                          description: |-
                            (String) Minimum log entry level.
                            Minimum log entry level.
                          type: string
                      type: object
                    type: array
                  memory:
                    description: |-
                      (Number) Memory in megabytes (aligned to 128MB) for Yandex Cloud Function.
                      Memory in megabytes (**aligned to 128MB**) for Yandex Cloud Function.
                    type: number
                  metadataOptions:
                    description: |-
                      (Block List, Max: 1) Options set the access mode to function's metadata endpoints. (see below for nested schema)
                      Options set the access mode to function's metadata endpoints.
                    items:
                      properties:
                        awsV1HttpEndpoint:
                          description: |-
                            default, 1 - enabled, 2 - disabled.
                            Enables access to AWS flavored metadata (IMDSv1). Values: `0` - default, `1` - enabled, `2` - disabled.
                          type: number
                        gceHttpEndpoint:
                          description: |-
                            default, 1 - enabled, 2 - disabled.
                            Enables access to GCE flavored metadata. Values: `0`- default, `1` - enabled, `2` - disabled.
                          type: number
                      type: object
                    type: array
                  mounts:
                    description: |-
                      (Block List) Mounts for Yandex Cloud Function. (see below for nested schema)
                      Mounts for Yandex Cloud Function.
                    items:
                      properties:
                        ephemeralDisk:
                          description: |-
                            (Block List, Max: 1) One of the available mount types. Disk available during the function execution time. (see below for nested schema)
                            One of the available mount types. Disk available during the function execution time.
                          items:
                            properties:
                              blockSizeKb:
                                description: |-
                                  (Number) Optional block size of the ephemeral disk in KB.
                                  Optional block size of the ephemeral disk in KB.
                                type: number
                              sizeGb:
                                description: |-
                                  (Number) Size of the ephemeral disk in GB.
                                  Size of the ephemeral disk in GB.
                                type: number
                            type: object
                          type: array
                        mode:
                          description: |-
                            (String) Mount’s accessibility mode. Valid values are ro and rw.
                            Mount’s accessibility mode. Valid values are `ro` and `rw`.
                          type: string
                        name:
                          description: |-
                            (String) The resource name.
                            Name of the mount point. The directory where the target is mounted will be accessible at the `/function/storage/<mounts.0.name>` path.
                          type: string
                        objectStorage:
                          description: |-
                            (Block List, Max: 1) One of the available mount types. Object storage as a mount. (see below for nested schema)
                            One of the available mount types. Object storage as a mount.
                          items:
                            properties:
                              bucket:
                                description: |-
                                  (String) Name of the mounting bucket.
                                  Name of the mounting bucket.
                                type: string
                              bucketRef:
                                description: Reference to a Bucket in storage to populate
                                  bucket.
                                properties:
                                  name:
                                    description: Name of the referenced object.
                                    type: string
                                  namespace:
                                    description: Namespace of the referenced object
                                    type: string
                                  policy:
                                    description: Policies for referencing.
                                    properties:
                                      resolution:
                                        default: Required
                                        description: |-
                                          Resolution specifies whether resolution of this reference is required.
                                          The default is 'Required', which means the reconcile will fail if the
                                          reference cannot be resolved. 'Optional' means this reference will be
                                          a no-op if it cannot be resolved.
                                        enum:
                                        - Required
                                        - Optional
                                        type: string
                                      resolve:
                                        description: |-
                                          Resolve specifies when this reference should be resolved. The default
                                          is 'IfNotPresent', which will attempt to resolve the reference only when
                                          the corresponding field is not present. Use 'Always' to resolve the
                                          reference on every reconcile.
                                        enum:
                                        - Always
                                        - IfNotPresent
                                        type: string
                                    type: object
                                required:
                                - name
                                type: object
                              bucketSelector:
                                description: Selector for a Bucket in storage to populate
                                  bucket.
                                properties:
                                  matchControllerRef:
                                    description: |-
                                      MatchControllerRef ensures an object with the same controller reference
                                      as the selecting object is selected.
                                    type: boolean
                                  matchLabels:
                                    additionalProperties:
                                      type: string
                                    description: MatchLabels ensures an object with
                                      matching labels is selected.
                                    type: object
                                  namespace:
                                    description: Namespace for the selector
                                    type: string
                                  policy:
                                    description: Policies for selection.
                                    properties:
                                      resolution:
                                        default: Required
                                        description: |-
                                          Resolution specifies whether resolution of this reference is required.
                                          The default is 'Required', which means the reconcile will fail if the
                                          reference cannot be resolved. 'Optional' means this reference will be
                                          a no-op if it cannot be resolved.
                                        enum:
                                        - Required
                                        - Optional
                                        type: string
                                      resolve:
                                        description: |-
                                          Resolve specifies when this reference should be resolved. The default
                                          is 'IfNotPresent', which will attempt to resolve the reference only when
                                          the corresponding field is not present. Use 'Always' to resolve the
                                          reference on every reconcile.
                                        enum:
                                        - Always
                                        - IfNotPresent
                                        type: string
                                    type: object
                                type: object
                              prefix:
                                description: |-
                                  (String) Prefix within the bucket. If you leave this field empty, the entire bucket will be mounted.
                                  Prefix within the bucket. If you leave this field empty, the entire bucket will be mounted.
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  name:
                    description: |-
                      (String) The resource name.
                      The resource name.
                    type: string
                  package:
                    description: |-
                      (Block List, Max: 1) Version deployment package for Yandex Cloud Function code. Can be only one package or content section. Either package or content section must be specified. (see below for nested schema)
                      Version deployment package for Yandex Cloud Function code. Can be only one `package` or `content` section. Either `package` or `content` section must be specified.
                    items:
                      properties:
                        bucketName:
                          description: |-
                            (String) Name of the bucket that stores the code for the version.
                            Name of the bucket that stores the code for the version.
                          type: string
                        objectName:
                          description: |-
                            (String) Name of the object in the bucket that stores the code for the version.
                            Name of the object in the bucket that stores the code for the version.
                          type: string
                        sha256:
                          description: |-
                            (String) SHA256 hash of the version deployment package.
                            SHA256 hash of the version deployment package.
                          type: string
                      type: object
                    type: array
                  runtime:
                    description: |-
                      (String) Runtime for Yandex Cloud Function.
                      Runtime for Yandex Cloud Function.
                    type: string
                  secrets:
                    description: |-
                      (Block List) Secrets for Yandex Cloud Function. (see below for nested schema)
                      Secrets for Yandex Cloud Function.
                    items:
                      properties:
                        environmentVariable:
                          description: |-
                            Z, a-z).
                            Function's environment variable in which secret's value will be stored. Must begin with a letter (A-Z, a-z).
                          type: string
                        id:
                          description: |-
                            (String) The ID of this resource.
                            Secret's ID.
                          type: string
                        idRef:
                          description: Reference to a Secret in lockbox to populate
                            id.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            namespace:
                              description: Namespace of the referenced object
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        idSelector:
                          description: Selector for a Secret in lockbox to populate
                            id.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            namespace:
                              description: Namespace for the selector
                              type: string
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                        key:
                          description: |-
                            (String) Secret's entries key which value will be stored in environment variable.
                            Secret's entries key which value will be stored in environment variable.
                          type: string
                        versionId:
                          description: |-
                            (String) Secret's version ID.
                            Secret's version ID.
                          type: string
                        versionIdRef:
                          description: Reference to a SecretVersion in lockbox to
                            populate versionId.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            namespace:
                              description: Namespace of the referenced object
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        versionIdSelector:
                          description: Selector for a SecretVersion in lockbox to
                            populate versionId.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            namespace:
                              description: Namespace for the selector
                              type: string
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                      type: object
                    type: array
                  serviceAccountId:
                    description: |-
                      (String) Service account which linked to the resource.
                      [Service account](https://yandex.cloud/docs/iam/concepts/users/service-accounts) which linked to the resource.
                    type: string
                  serviceAccountIdRef:
                    description: Reference to a ServiceAccount in iam to populate
                      serviceAccountId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  serviceAccountIdSelector:
                    description: Selector for a ServiceAccount in iam to populate
                      serviceAccountId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  storageMounts:
                    description: |-
                      > object_storage instead). Storage mounts for Yandex Cloud Function. (see below for nested schema)
                      (**DEPRECATED**, use `mounts -> object_storage` instead). Storage mounts for Yandex Cloud Function.
                    items:
                      properties:
                        bucket:
                          description: |-
                            (String) Name of the mounting bucket.
                            Name of the mounting bucket.
                          type: string
                        mountPointName:
                          description: |-
                            (String) Name of the mount point. The directory where the bucket is mounted will be accessible at the /function/storage/<mount_point> path.
                            Name of the mount point. The directory where the bucket is mounted will be accessible at the `/function/storage/<mount_point>` path.
                          type: string
                        prefix:
                          description: |-
                            (String) Prefix within the bucket. If you leave this field empty, the entire bucket will be mounted.
                            Prefix within the bucket. If you leave this field empty, the entire bucket will be mounted.
                          type: string
                        readOnly:
                          description: |-
                            only mode.
                            Mount the bucket in read-only mode.
                          type: boolean
                      type: object
                    type: array
                  tags:
                    description: |-
                      (Set of String) Tags for Yandex Cloud Function. Tag $latest isn't returned.
                      Tags for Yandex Cloud Function. Tag `$latest` isn't returned.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  tmpfsSize:
                    description: |-
                      (Number) Tmpfs size for Yandex Cloud Function.
                      Tmpfs size for Yandex Cloud Function.
                    type: number
                  userHash:
                    description: |-
                      defined string for current function version. User must change this string any times when function changed. Function will be updated when hash is changed.
                      User-defined string for current function version. User must change this string any times when function changed. Function will be updated when hash is changed.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.entrypoint is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.entrypoint)
                || (has(self.initProvider) && has(self.initProvider.entrypoint))'
            - message: spec.forProvider.memory is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.memory)
                || (has(self.initProvider) && has(self.initProvider.memory))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.runtime is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.runtime)
                || (has(self.initProvider) && has(self.initProvider.runtime))'
            - message: spec.forProvider.userHash is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.userHash)
                || (has(self.initProvider) && has(self.initProvider.userHash))'
          status:
            description: FunctionStatus defines the observed state of Function.
            properties:
              atProvider:
                properties:
                  asyncInvocation:
                    description: |-
                      (Block List, Max: 1) Config for asynchronous invocations of Yandex Cloud Function. (see below for nested schema)
                      Config for asynchronous invocations of Yandex Cloud Function.
                    items:
                      properties:
                        retriesCount:
                          description: |-
                            (Number) Maximum number of retries for async invocation.
                            Maximum number of retries for async invocation.
                          type: number
                        serviceAccountId:
                          description: |-
                            (String) Service account which linked to the resource.
                            Service account used for async invocation.
                          type: string
                        ymqFailureTarget:
                          description: |-
                            (Block List, Max: 1) Target for unsuccessful async invocation. (see below for nested schema)
                            Target for unsuccessful async invocation.
                          items:
                            properties:
                              arn:
                                description: |-
                                  (String) YMQ ARN.
                                  YMQ ARN.
                                type: string
                              serviceAccountId:
                                description: |-
                                  (String) Service account which linked to the resource.
                                  Service account used for writing result to queue.
                                type: string
                            type: object
                          type: array
                        ymqSuccessTarget:
                          description: |-
                            (Block List, Max: 1) Target for successful async invocation. (see below for nested schema)
                            Target for successful async invocation.
                          items:
                            properties:
                              arn:
                                description: |-
                                  (String) YMQ ARN.
                                  YMQ ARN.
                                type: string
                              serviceAccountId:
                                description: |-
                                  (String) Service account which linked to the resource.
                                  Service account used for writing result to queue.
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  concurrency:
                    description: |-
                      (Number) The maximum number of requests processed by a function instance at the same time.
                      The maximum number of requests processed by a function instance at the same time.
                    type: number
                  connectivity:
                    description: |-
                      (Block List, Max: 1) Function version connectivity. If specified the version will be attached to specified network. (see below for nested schema)
                      Function version connectivity. If specified the version will be attached to specified network.
                    items:
                      properties:
                        networkId:
                          description: |-
                            (String) Network the version will have access to. It's essential to specify network with subnets in all availability zones.
                            Network the version will have access to. It's essential to specify network with subnets in all availability zones.
                          type: string
                      type: object
                    type: array
                  content:
                    description: |-
                      (Block List, Max: 1) Version deployment content for Yandex Cloud Function code. Can be only one package or content section. Either package or content section must be specified. (see below for nested schema)
                      Version deployment content for Yandex Cloud Function code. Can be only one `package` or `content` section. Either `package` or `content` section must be specified.
                    items:
                      properties:
                        zipFilename:
                          description: |-
                            (String) Filename to zip archive for the version.
                            Filename to zip archive for the version.
                          type: string
                      type: object
                    type: array
                  createdAt:
                    description: (String)
                    type: string
                  description:
                    description: |-
                      (String) The resource description.
                      The resource description.
                    type: string
                  entrypoint:
                    description: |-
                      (String) Entrypoint for Yandex Cloud Function.
                      Entrypoint for Yandex Cloud Function.
                    type: string
                  environment:
                    additionalProperties:
                      type: string
                    description: |-
                      Z, a-z).
                      A set of key/value environment variables for Yandex Cloud Function. Each key must begin with a letter (A-Z, a-z).
                    type: object
                    x-kubernetes-map-type: granular
                  executionTimeout:
                    description: |-
                      (String) Execution timeout in seconds for Yandex Cloud Function.
                      Execution timeout in seconds for Yandex Cloud Function.
                    type: string
                  folderId:
                    description: |-
                      id is used.
                      The folder identifier that resource belongs to. If it is not provided, the default provider `folder-id` is used.
                    type: string
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  imageSize:
                    description: |-
                      (Number) Image size for Yandex Cloud Function.
                      Image size for Yandex Cloud Function.
                    type: number
                  labels:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) A set of key/value label pairs which assigned to resource.
                      A set of key/value label pairs which assigned to resource.
                    type: object
                    x-kubernetes-map-type: granular
                  logOptions:
                    description: |-
                      (Block List, Max: 1) Options for logging from Yandex Cloud Function. (see below for nested schema)
                      Options for logging from Yandex Cloud Function.
                    items:
                      properties:
                        disabled:
                          description: |-
                            (Boolean) Is logging from function disabled.
                            Is logging from function disabled.
                          type: boolean
                        folderId:
                          description: |-
                            id is used.
                            Log entries are written to default log group for specified folder.
                          type: string
                        logGroupId:
                          description: |-
                            (String) Log entries are written to specified log group.
                            Log entries are written to specified log group.
                          type: string
                        minLevel:
                          description: |-
                            (String) Minimum log entry level.
                            Minimum log entry level.
                          type: string
                      type: object
                    type: array
                  memory:
                    description: |-
                      (Number) Memory in megabytes (aligned to 128MB) for Yandex Cloud Function.
                      Memory in megabytes (**aligned to 128MB**) for Yandex Cloud Function.
                    type: number
                  metadataOptions:
                    description: |-
                      (Block List, Max: 1) Options set the access mode to function's metadata endpoints. (see below for nested schema)
                      Options set the access mode to function's metadata endpoints.
                    items:
                      properties:
                        awsV1HttpEndpoint:
                          description: |-
                            default, 1 - enabled, 2 - disabled.
                            Enables access to AWS flavored metadata (IMDSv1). Values: `0` - default, `1` - enabled, `2` - disabled.
                          type: number
                        gceHttpEndpoint:
                          description: |-
                            default, 1 - enabled, 2 - disabled.
                            Enables access to GCE flavored metadata. Values: `0`- default, `1` - enabled, `2` - disabled.
                          type: number
                      type: object
                    type: array
                  mounts:
                    description: |-
                      (Block List) Mounts for Yandex Cloud Function. (see below for nested schema)
                      Mounts for Yandex Cloud Function.
                    items:
                      properties:
                        ephemeralDisk:
                          description: |-
                            (Block List, Max: 1) One of the available mount types. Disk available during the function execution time. (see below for nested schema)
                            One of the available mount types. Disk available during the function execution time.
                          items:
                            properties:
                              blockSizeKb:
                                description: |-
                                  (Number) Optional block size of the ephemeral disk in KB.
                                  Optional block size of the ephemeral disk in KB.
                                type: number
                              sizeGb:
                                description: |-
                                  (Number) Size of the ephemeral disk in GB.
                                  Size of the ephemeral disk in GB.
                                type: number
                            type: object
                          type: array
                        mode:
                          description: |-
                            (String) Mount’s accessibility mode. Valid values are ro and rw.
                            Mount’s accessibility mode. Valid values are `ro` and `rw`.
                          type: string
                        name:
                          description: |-
                            (String) The resource name.
                            Name of the mount point. The directory where the target is mounted will be accessible at the `/function/storage/<mounts.0.name>` path.
                          type: string
                        objectStorage:
                          description: |-
                            (Block List, Max: 1) One of the available mount types. Object storage as a mount. (see below for nested schema)
                            One of the available mount types. Object storage as a mount.
                          items:
                            properties:
                              bucket:
                                description: |-
                                  (String) Name of the mounting bucket.
                                  Name of the mounting bucket.
                                type: string
                              prefix:
                                description: |-
                                  (String) Prefix within the bucket. If you leave this field empty, the entire bucket will be mounted.
                                  Prefix within the bucket. If you leave this field empty, the entire bucket will be mounted.
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  name:
                    description: |-
                      (String) The resource name.
                      The resource name.
                    type: string
                  package:
                    description: |-
                      (Block List, Max: 1) Version deployment package for Yandex Cloud Function code. Can be only one package or content section. Either package or content section must be specified. (see below for nested schema)
                      Version deployment package for Yandex Cloud Function code. Can be only one `package` or `content` section. Either `package` or `content` section must be specified.
                    items:
                      properties:
                        bucketName:
                          description: |-
                            (String) Name of the bucket that stores the code for the version.
                            Name of the bucket that stores the code for the version.
                          type: string
                        objectName:
                          description: |-
                            (String) Name of the object in the bucket that stores the code for the version.
                            Name of the object in the bucket that stores the code for the version.
                          type: string
                        sha256:
                          description: |-
                            (String) SHA256 hash of the version deployment package.
                            SHA256 hash of the version deployment package.
                          type: string
                      type: object
                    type: array
                  runtime:
                    description: |-
                      (String) Runtime for Yandex Cloud Function.
                      Runtime for Yandex Cloud Function.
                    type: string
                  secrets:
                    description: |-
                      (Block List) Secrets for Yandex Cloud Function. (see below for nested schema)
                      Secrets for Yandex Cloud Function.
                    items:
                      properties:
                        environmentVariable:
                          description: |-
                            Z, a-z).
                            Function's environment variable in which secret's value will be stored. Must begin with a letter (A-Z, a-z).
                          type: string
                        id:
                          description: |-
                            (String) The ID of this resource.
                            Secret's ID.
                          type: string
                        key:
                          description: |-
                            (String) Secret's entries key which value will be stored in environment variable.
                            Secret's entries key which value will be stored in environment variable.
                          type: string
                        versionId:
                          description: |-
                            (String) Secret's version ID.
                            Secret's version ID.
                          type: string
                      type: object
                    type: array
                  serviceAccountId:
                    description: |-
                      (String) Service account which linked to the resource.
                      [Service account](https://yandex.cloud/docs/iam/concepts/users/service-accounts) which linked to the resource.
                    type: string
                  storageMounts:
                    description: |-
                      > object_storage instead). Storage mounts for Yandex Cloud Function. (see below for nested schema)
                      (**DEPRECATED**, use `mounts -> object_storage` instead). Storage mounts for Yandex Cloud Function.
                    items:
                      properties:
                        bucket:
                          description: |-
                            (String) Name of the mounting bucket.
                            Name of the mounting bucket.
                          type: string
                        mountPointName:
                          description: |-
                            (String) Name of the mount point. The directory where the bucket is mounted will be accessible at the /function/storage/<mount_point> path.
                            Name of the mount point. The directory where the bucket is mounted will be accessible at the `/function/storage/<mount_point>` path.
                          type: string
                        prefix:
                          description: |-
                            (String) Prefix within the bucket. If you leave this field empty, the entire bucket will be mounted.
                            Prefix within the bucket. If you leave this field empty, the entire bucket will be mounted.
                          type: string
                        readOnly:
                          description: |-
                            only mode.
                            Mount the bucket in read-only mode.
                          type: boolean
                      type: object
                    type: array
                  tags:
                    description: |-
                      (Set of String) Tags for Yandex Cloud Function. Tag $latest isn't returned.
                      Tags for Yandex Cloud Function. Tag `$latest` isn't returned.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  tmpfsSize:
                    description: |-
                      (Number) Tmpfs size for Yandex Cloud Function.
                      Tmpfs size for Yandex Cloud Function.
                    type: number
                  userHash:
                    description: |-
                      defined string for current function version. User must change this string any times when function changed. Function will be updated when hash is changed.
                      User-defined string for current function version. User must change this string any times when function changed. Function will be updated when hash is changed.
                    type: string
                  version:
                    description: |-
                      (String) Version of Yandex Cloud Function.
                      Version of Yandex Cloud Function.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
