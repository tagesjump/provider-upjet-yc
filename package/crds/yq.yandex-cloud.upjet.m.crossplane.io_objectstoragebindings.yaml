---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: objectstoragebindings.yq.yandex-cloud.upjet.m.crossplane.io
spec:
  group: yq.yandex-cloud.upjet.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - yandex-cloud
    kind: ObjectStorageBinding
    listKind: ObjectStorageBindingList
    plural: objectstoragebindings
    singular: objectstoragebinding
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ObjectStorageBinding is the Schema for the ObjectStorageBindings
          API. Manages Object Storage binding.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ObjectStorageBindingSpec defines the desired state of ObjectStorageBinding
            properties:
              forProvider:
                properties:
                  column:
                    description: (Block List) (see below for nested schema)
                    items:
                      properties:
                        name:
                          description: |-
                            (String) The resource name.
                            Column name.
                          type: string
                        notNull:
                          description: |-
                            (Boolean) A column cannot have the NULL data type. Default: false.
                            A column cannot have the NULL data type. Default: `false`.
                          type: boolean
                        type:
                          description: |-
                            (String) Column data type. YQL data types are used.
                            Column data type. YQL data types are used.
                          type: string
                      type: object
                    type: array
                  compression:
                    description: |-
                      (String) The data compression algorithm, e.g. brotli, bzip2, gzip, lz4, xz, zstd.
                      The data compression algorithm, e.g. brotli, bzip2, gzip, lz4, xz, zstd.
                    type: string
                  connectionId:
                    description: |-
                      (String) The connection identifier.
                      The connection identifier.
                    type: string
                  connectionIdRef:
                    description: Reference to a ObjectStorageConnection in yq to populate
                      connectionId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  connectionIdSelector:
                    description: Selector for a ObjectStorageConnection in yq to populate
                      connectionId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  description:
                    description: |-
                      (String) The resource description.
                      The resource description.
                    type: string
                  format:
                    description: |-
                      (String) The data format, e.g. csv_with_names, json_as_string, json_each_row, json_list, parquet, raw, tsv_with_names.
                      The data format, e.g. csv_with_names, json_as_string, json_each_row, json_list, parquet, raw, tsv_with_names.
                    type: string
                  formatSetting:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Special format setting.
                      Special format setting.
                    type: object
                    x-kubernetes-map-type: granular
                  name:
                    description: |-
                      (String) The resource name.
                      The resource name.
                    type: string
                  partitionedBy:
                    description: |-
                      (List of String) The list of partitioning column names.
                      The list of partitioning column names.
                    items:
                      type: string
                    type: array
                  pathPattern:
                    description: |-
                      (String) The path pattern within Object Storage's bucket.
                      The path pattern within Object Storage's bucket.
                    type: string
                  projection:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Projection rules.
                      Projection rules.
                    type: object
                    x-kubernetes-map-type: granular
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  column:
                    description: (Block List) (see below for nested schema)
                    items:
                      properties:
                        name:
                          description: |-
                            (String) The resource name.
                            Column name.
                          type: string
                        notNull:
                          description: |-
                            (Boolean) A column cannot have the NULL data type. Default: false.
                            A column cannot have the NULL data type. Default: `false`.
                          type: boolean
                        type:
                          description: |-
                            (String) Column data type. YQL data types are used.
                            Column data type. YQL data types are used.
                          type: string
                      type: object
                    type: array
                  compression:
                    description: |-
                      (String) The data compression algorithm, e.g. brotli, bzip2, gzip, lz4, xz, zstd.
                      The data compression algorithm, e.g. brotli, bzip2, gzip, lz4, xz, zstd.
                    type: string
                  connectionId:
                    description: |-
                      (String) The connection identifier.
                      The connection identifier.
                    type: string
                  connectionIdRef:
                    description: Reference to a ObjectStorageConnection in yq to populate
                      connectionId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  connectionIdSelector:
                    description: Selector for a ObjectStorageConnection in yq to populate
                      connectionId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  description:
                    description: |-
                      (String) The resource description.
                      The resource description.
                    type: string
                  format:
                    description: |-
                      (String) The data format, e.g. csv_with_names, json_as_string, json_each_row, json_list, parquet, raw, tsv_with_names.
                      The data format, e.g. csv_with_names, json_as_string, json_each_row, json_list, parquet, raw, tsv_with_names.
                    type: string
                  formatSetting:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Special format setting.
                      Special format setting.
                    type: object
                    x-kubernetes-map-type: granular
                  name:
                    description: |-
                      (String) The resource name.
                      The resource name.
                    type: string
                  partitionedBy:
                    description: |-
                      (List of String) The list of partitioning column names.
                      The list of partitioning column names.
                    items:
                      type: string
                    type: array
                  pathPattern:
                    description: |-
                      (String) The path pattern within Object Storage's bucket.
                      The path pattern within Object Storage's bucket.
                    type: string
                  projection:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Projection rules.
                      Projection rules.
                    type: object
                    x-kubernetes-map-type: granular
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.format is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.format)
                || (has(self.initProvider) && has(self.initProvider.format))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.pathPattern is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.pathPattern)
                || (has(self.initProvider) && has(self.initProvider.pathPattern))'
          status:
            description: ObjectStorageBindingStatus defines the observed state of
              ObjectStorageBinding.
            properties:
              atProvider:
                properties:
                  column:
                    description: (Block List) (see below for nested schema)
                    items:
                      properties:
                        name:
                          description: |-
                            (String) The resource name.
                            Column name.
                          type: string
                        notNull:
                          description: |-
                            (Boolean) A column cannot have the NULL data type. Default: false.
                            A column cannot have the NULL data type. Default: `false`.
                          type: boolean
                        type:
                          description: |-
                            (String) Column data type. YQL data types are used.
                            Column data type. YQL data types are used.
                          type: string
                      type: object
                    type: array
                  compression:
                    description: |-
                      (String) The data compression algorithm, e.g. brotli, bzip2, gzip, lz4, xz, zstd.
                      The data compression algorithm, e.g. brotli, bzip2, gzip, lz4, xz, zstd.
                    type: string
                  connectionId:
                    description: |-
                      (String) The connection identifier.
                      The connection identifier.
                    type: string
                  description:
                    description: |-
                      (String) The resource description.
                      The resource description.
                    type: string
                  format:
                    description: |-
                      (String) The data format, e.g. csv_with_names, json_as_string, json_each_row, json_list, parquet, raw, tsv_with_names.
                      The data format, e.g. csv_with_names, json_as_string, json_each_row, json_list, parquet, raw, tsv_with_names.
                    type: string
                  formatSetting:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Special format setting.
                      Special format setting.
                    type: object
                    x-kubernetes-map-type: granular
                  id:
                    description: (String) The resource identifier.
                    type: string
                  name:
                    description: |-
                      (String) The resource name.
                      The resource name.
                    type: string
                  partitionedBy:
                    description: |-
                      (List of String) The list of partitioning column names.
                      The list of partitioning column names.
                    items:
                      type: string
                    type: array
                  pathPattern:
                    description: |-
                      (String) The path pattern within Object Storage's bucket.
                      The path pattern within Object Storage's bucket.
                    type: string
                  projection:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Projection rules.
                      Projection rules.
                    type: object
                    x-kubernetes-map-type: granular
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
